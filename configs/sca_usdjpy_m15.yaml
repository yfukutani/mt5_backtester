# SCA_EA（セッションORBスキャルパー）— USDJPY M15
# アジア時間(0-9h鯖時間)レンジをロンドン午前(9-12h)にブレイクした方向へ日中完結で取る。
# 最終形(2026.07.03): TE12/RR2.0/buf0.05/MinRange0.30。全期間+8,589/PF1.05/DD10.2%(every_tick)。
# ⚠️ IS(2021-26)+9,210/OOS(2016-21)-621＝利益はISドリブン・OOSフラット。GOLDが主軸、本枠は補助。
# ⚠️ スキャルピングのため検証は必ず every_tick で行うこと（open_prices不可）。
mt5_path: "C:\\Users\\f\\AppData\\Roaming\\XMTrading MT5\\terminal64.exe"

expert:    "SCA_EA"
symbol:    "USDJPY"
period:    "M15"
from_date: "2016.06.21"
to_date:   "2026.06.20"

deposit:  100000
currency: "JPY"
leverage: 25

model: "every_tick"

parameters:
  RangeStartHour:    0
  RangeEndHour:      9
  TradeEndHour:      12
  ForceCloseHour:    22
  MinRange_ATRd:     0.30
  MaxRange_ATRd:     1.00
  Break_Buffer_ATRd: 0.05
  SL_Mode:           0
  SL_ATRd_Mult:      0.5
  RR_Ratio:          2.0
  OneShotPerDir:     true
  UseD1TrendFilter:  false
  MaxSpreadPoints:   0
  LotSize:           0.01
  UseRiskSizing:     false
  MagicNumber:       20261000
  ResultFileName:    "sca_usdjpy_m15_result.csv"

report_dir:  "results"
report_name: "SCA_USDJPY_M15"