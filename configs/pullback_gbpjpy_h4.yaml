# 押し目買い/戻り売り トレンドフォローEA — GBPJPY H4（円ペアポートフォリオ）
# PullbackTrend v1.2: MA200方向 + EMA20/50トレンド + EMA20押し目反発 + 環境フィルター
# 環境フィルターON必須（OFFだとOOS壊滅）。円ペアポートフォリオの一角。
# 成績(MTFフィルター前, 固定): IS(2021-2026)+491/DD8.3% / OOS(2016-2021)+8,423/DD8.6% / Full+8,914
# v1.3: マルチタイムフレーム合流フィルター(UseHigherTFFilter)を追加採用。上位足(D1)のMA200トレンドが
#   H4と一致する場合のみエントリー許可。固定ロット全期間+8,914→+9,924(+11%)、DD8.63%→7.74%に低下。
#   ISは+491のまま不変(D1/H4が常に一致=フィルター非発動)、OOSは+8,423→+9,433に改善。
#   詳細: docs/rejected_strategies.md（新規戦略候補#9の検証記録）
# サイジング: リスク2%採用。MTFフィルター込み risk2%: +18,337/PF1.20/DD10.74%
#   （旧risk2% +17,263/PF1.18/DD11.6%から改善）。PF>1の真のエッジがあるためサイジングが有効
#   （docs/position_sizing.md）。
# v1.4(2026-08): MA_Slope_Min_ATR 1.2→1.5, RR_Ratio 2.0→3.5採用（応答曲面M366・本番同一条件
#   tier2確認: IS-90→+18,665／OOS+13,254→+4,641。現状市場(IS)の利益を優先しユーザー承認、
#   OOS低下（頑健性低下）は許容という判断。docs/new_strategies_round2_20260805.md）
# v1.5(2026-08): RR_Ratio 3.5→4.0採用（Codex提案11の近傍応答曲面・本番同一条件tier2確認:
#   IS+18,665→+29,315／OOS+4,641→+10,197。トレードオフなしの純改善。
#   docs/codex_verification_20260808.md）
# v1.6(2026-08-10): ADX_Period 14→10採用（Codex 500案応答曲面C・IS+29,315→+31,611/
#   OOS+10,197→+14,670。トレードオフなしの純改善。docs/codex500_verification_20260810.md）
# v1.7(2026-08-10): EMA組 20/50→25/60採用（Codex 500案残余F5・IS+31,611→+33,133/
#   OOS+14,670→+20,763・DD18.13→14.68%＝トレードオフなしの純改善。
#   docs/codex500_verification2_20260810.md）
# ⚠️構造TP(UseStructureTP)はF9でEMA20/50時にOOS改善(+14,670→+18,475)を示したため一度採用したが、
#   EMA25/60採用後の実測でON/OFFの結果が完全一致＝**発火せず無効**と判明したため既定OFFに戻した
#   （MinRR1.5では構造距離が1.5*SL〜4.0*SLの窓に入る取引が皆無。MinRR0.5まで下げると発火するが
#   IS+33,133→+15,833/OOS+20,763→+435と激しく悪化）。EAへの実装自体は温存。
mt5_path: "C:\\Users\\f\\AppData\\Roaming\\XMTrading MT5\\terminal64.exe"

expert:    "PullbackTrend"
symbol:    "GBPJPY"
period:    "H4"
from_date: "2016.06.21"
to_date:   "2026.06.20"

deposit:  100000
currency: "JPY"
leverage: 25

model: "open_prices"

parameters:
  TrendMA_Period:          200
  FastEMA_Period:          25
  SlowEMA_Period:          35
  RequireBullishCandle:    true
  UsePullbackQuality:      true
  UseMomentumConfirm:      true
  UseADXFilter:            true
  ADX_Period:              10
  ADX_Threshold:           30.0
  UseTrendStrength:        true
  MA_Slope_Lookback:       20
  MA_Slope_Min_ATR:        1.5
  UseATRStops:             true
  ATR_Period:              14
  ATR_SL_Mult:             2.0
  RR_Ratio:                4.0
  StopLoss_Pips:           50
  TakeProfit_Pips:         110
  LotSize:                 0.01
  UseRiskSizing:           true
  RiskPercent:             2.0
  UseHigherTFFilter:       true
  HigherTF:                16408
  HigherTF_MA:             200
  MagicNumber:             20260627
  ResultFileName:          "pullback_gbpjpy_h4_result.csv"

report_dir:  "results"
report_name: "PullbackTrend_GBPJPY_H4"
