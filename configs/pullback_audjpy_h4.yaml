# ⚠️ レガシー（本番デプロイ除外）: 純利益+44で実質エッジ無し、他円ペアと冗長。本番25万構成からは除外（docs/portfolio.md）。
# 押し目買い/戻り売り トレンドフォローEA — AUDJPY H4（円ペアポートフォリオ）
# PullbackTrend v1.2: MA200方向 + EMA20/50トレンド + EMA20押し目反発 + 環境フィルター
# 環境フィルターON必須（OFFだとOOS壊滅）。円ペアポートフォリオの分散要員。
# 成績(固定): IS(2021-2026)+2,842/DD2.2% / OOS(2016-2021)-2,798/DD8.6% / Full+44（分散効果）
# サイジング: 固定維持。全期間 固定+44/PF1.00（実質エッジ無し）。risk2%だと+44→-4,512に負転落。
#   エッジ無し戦略への複利はボラドラッグでマイナス化するため固定（docs/position_sizing.md）。
mt5_path: "C:\\Users\\f\\AppData\\Roaming\\XMTrading MT5\\terminal64.exe"

expert:    "PullbackTrend"
symbol:    "AUDJPY"
period:    "H4"
from_date: "2016.06.21"
to_date:   "2026.06.20"

deposit:  100000
currency: "JPY"
leverage: 25

model: "open_prices"

parameters:
  TrendMA_Period:          200
  FastEMA_Period:          20
  SlowEMA_Period:          50
  RequireBullishCandle:    true
  UsePullbackQuality:      true
  UseMomentumConfirm:      true
  UseADXFilter:            true
  ADX_Period:              14
  ADX_Threshold:           22.5
  UseTrendStrength:        true
  MA_Slope_Lookback:       20
  MA_Slope_Min_ATR:        1.2
  UseATRStops:             true
  ATR_Period:              14
  ATR_SL_Mult:             2.0
  RR_Ratio:                2.0
  StopLoss_Pips:           50
  TakeProfit_Pips:         110
  LotSize:                 0.01
  UseRiskSizing:           false
  RiskPercent:             2.0
  MagicNumber:             20260628
  ResultFileName:          "pullback_audjpy_h4_result.csv"

report_dir:  "results"
report_name: "PullbackTrend_AUDJPY_H4"
