# SCA_EA（セッションORBスキャルパー）— GBPJPY M15
# アジア時間(0-9h鯖時間)レンジをロンドン午前(9-12h)にブレイクした方向へ日中完結で取る。
# 最終形(2026.07.03): TE12/RR2.0/buf0/MinRange0.30。全期間+15,327/PF1.04/DD17.6%(every_tick)。
# ⚠️ IS(2021-26)+15,880/OOS(2016-21)-553＝利益はISドリブン・OOSフラット。GOLDが主軸、本枠は補助。
# v1.1(2026-08): Boost_Mult 2.0→3.0採用（応答曲面M239・本番同一条件tier2確認:
#   IS+27,445→+39,027/OOS+11,127→+23,451、docs/new_strategies_round2_20260805.md）
# v1.2(2026-08-10): Boost_Mult 3.0→4.0採用（Codex 500案残余F21・IS+39,027→+50,609/
#   OOS+23,451→+35,775）。⚠️利益+30〜53%と引き換えにDDも悪化（IS 24.01→26.45%・
#   OOS 17.37→19.32%）＝利益とリスクのトレードオフをユーザー承認のうえ採用。
#   docs/codex500_verification2_20260810.md
# v1.3(2026-08-11): Boost_Mult 4.0→6.0採用（Codex 500案ラウンド3 R3F09・
#   IS+50,609→+73,773/OOS+35,775→+60,423＝利益+46〜69%）。
#   ⚠️DDも悪化（IS 26.45→30.03%・OOS 19.32→22.95%）。ただし取引数は全水準で完全同一
#   （IS684/OOS658）＝エントリー集合不変のサイジング変更でありながら利益/DD比は単調改善
#   （IS1913→2457・OOS1851→2632）。Boost対象が「レンジ内ドリフトと逆方向のブレイク」という
#   高エッジの部分集合のため、そこへ資金を寄せるのはリスク調整後も合理的。
#   ⚠️IS-DD30.03%はDD30%制約に接触（枠単独・0.01ロット基準。ポートフォリオでは配分比率がかかる）。
#   ⚠️Boost4.5は0.01×4.5=0.045がロットステップ0.01で0.04へ丸められ4.0と完全同値＝
#   0.01ロット基準では整数倍しか意味を持たない。docs/codex500_round3_20260811.md
# ⚠️ スキャルピングのため検証は必ず every_tick で行うこと（open_prices不可）。
mt5_path: "C:\\Users\\f\\AppData\\Roaming\\XMTrading MT5\\terminal64.exe"

expert:    "SCA_EA"
symbol:    "GBPJPY"
period:    "M15"
from_date: "2016.06.21"
to_date:   "2026.06.20"

deposit:  100000
currency: "JPY"
leverage: 25

model: "every_tick"

parameters:
  RangeStartHour:    0
  RangeEndHour:      9
  TradeEndHour:      12
  ForceCloseHour:    22
  MinRange_ATRd:     0.30
  MaxRange_ATRd:     1.00
  Break_Buffer_ATRd: 0.0
  SL_Mode:           0
  SL_ATRd_Mult:      0.5
  RR_Ratio:          2.0
  OneShotPerDir:     true
  UseD1TrendFilter:  false
  MaxSpreadPoints:   0
  LotSize:           0.01
  UseRiskSizing:     false
  MagicNumber:       20261001
  UseReversalBoost:  true
  Boost_Mult:        6.0
  ResultFileName:    "sca_GBPJPY_m15_result.csv"

report_dir:  "results"
report_name: "SCA_GBPJPY_M15"