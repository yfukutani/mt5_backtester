# mt5_backtester — プロジェクト運用ルール

## ドキュメント管理（重要・必読）

このプロジェクトのドキュメントは2層構造。

1. **`docs/*.md`（このリポジトリ内・append-only）** — 検証の一次記録。生データ・詳細な検証過程・
   検証記録（`*_verification.md`）はここに書く。削除・書き換えはしない（否定的な結果も記録として残す）。
2. **Obsidian Vault（`C:\project\obsidian\AI用\EA\`）** — **生きた索引・要約層。**
   「今どの戦略が採用されていて、何が棄却済みで、実績はどうなっているか」を素早く追える形に
   整理したもの。ノートは`[[wikilink]]`で相互に繋がっている。

**検証・実験が1つ完了するたびに、その場でObsidian Vaultも更新すること。** リポジトリのdocsに
書いて終わりにしない。具体的には:

- 数値が変わった（新しいバックテスト結果・フォワードテスト結果） → 該当EA/枠ノートの実績テーブルを更新
- 採用/棄却が決まった → 枠ノートを新規作成 or `棄却戦略/`にノートを作成し、MOCに追記
- 理論的な誤り・訂正が見つかった → `検証記録/`に新規ノートを作成（元の記述は`⚠️訂正`で残す。削除しない）
- 要件が変わった → 該当EAのトップノート（`X2_HIGH_RISK.md`など）の要件セクションを更新

Vaultの構造・ノートの型・詳細な運用ルールは、Vault自身の
`テンプレート/ドキュメント運用ルール.md` と `テンプレート/EAテンプレート.md` を参照
（Vaultを開いて読むこと。ここには要約のみ記載）。

**このVaultはmt5_backtester専用ではなく、ユーザーの他プロジェクトとも共有の汎用ナレッジベース。**
`EA/`はこのプロジェクト用のトップレベルフォルダで、他プロジェクトは並列に別フォルダを持つ。

## 戦略検証の進め方（プロジェクト全体のルール・2026-09-13 ユーザー指示）

> 検証の際ですが案の理論的な検証は簡略化していいですが、**採用の最終判断のための
> バックテストは必ず行ってください。** 検証の進め方の推奨ですが、戦略に対して
> **50案出しを行い簡易検証を行い、効果が出そうであればその案をバックテストで検証する**
> という流れで進めてください。これはこのプロジェクト全体でいえることです。

### 3段階で進める

| 段階 | やること | 通過条件 | かける手間 |
|---|---|---|---|
| **1. 案出し** | 1つの課題に対して**50案**を出す。Codexにも出させる | — | 軽く |
| **2. 簡易検証** | 既存の取引ログ・数値計算で効果の有無だけ見る。**理論的な検証は簡略化してよい** | 効果が出そうか | 軽く |
| **3. バックテスト** | **MT5で実際にバックテストを回す** | ここを通らないものは**採用しない** | 重く・必ず |

### 絶対のルール

> [!important] **採用の最終判断は、必ずMT5バックテストで行う。**
> 取引ログの再サンプリング・数値シミュレーション・解析近似は、
> **段階2（ふるい分け）までの道具であって、採用の根拠にはならない。**
> 「シミュレーションで◯◯%だった」は採用理由として不十分。
> **EAを実装し、MT5でバックテストを回し、その結果で判断すること。**

### 注意

- 段階1で50案出すのは、**少数の案に固執して同じ軸を繰り返し調整するのを防ぐため。**
  過去に「7軸を順に潰す」進め方をして、探索が狭かったことがある。
- 段階2で落とした案も**記録に残す**（`docs/*.md` は append-only）。
- 段階2を通ったのに段階3で落ちた案は、**なぜ簡易検証が外したのか**を記録する。
  簡易検証の妥当性そのものの検証になる。
- IS窓の結果も必ず併記する（X2_HIGH_RISKのユーザー指示）。

## 開発時の注意

- `backtest.ini` は平文の認証情報（XM口座番号含む）を保持しており、**コミット厳禁・読み取りも避ける**。
- このリポジトリ（`yfukutani/mt5_backtester`）は **Public**。push前に認証情報・実口座残高が
  含まれていないか確認すること。
- MT5並列実行の上限はこのマシンでは2端末（XAUUSD every_tickは1テスターあたり約6GB）。
