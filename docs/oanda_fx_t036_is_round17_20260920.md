# 第17ラウンド — T036系は IS 窓でも生き残るのか（OANDA BT1・`ml/fxoanda4`・18run）

> 2026-09-20 記載。**本書を書いた時点でバックテストは1本も走っていない。**
> 設計・事前予想・Codex との突き合わせまでが本書の内容で、
> 結果は同ファイルに追記する（append-only）。
> 実装: `ml/fxoanda4/measure.py`（18 job）・`ml/fxoanda4/chain.ps1`（ゲート付き）。
> Codex の回答原文: [codex_oafx_round17_20260920.md](codex_oafx_round17_20260920.md)。

## 0. なぜこのラウンドをやるのか

主題は引き続き **枠ごと（戦略ごと）の質の改善**である。ただし第16報で
Codex が「B は該当なし。未測定軸として再提案できるものはありません」と答え、
**全複利ブックの上での枠の質は13ラウンド・約100案・224run で打ち止め**になっている。

そこで本ラウンドは、**同じ枠の質の軸を「別のブックの上」で測り直す**。理由は2つ。

1. 🔴 [OANDA 端末 cap 掃引](oanda_fx_terminal_cap_sweep_20260919.md) で見つかった
   **T036系 cap50 は OOS 5.537%/月**と、全複利ブックの最良（OOS 3.849%）より 1.7pt 高い。
   にもかかわらず **IS 窓を一度も測っていない**ので採用候補にできていない。
   **「実行可能な最良」が確定していない。**
2. 第1〜13ラウンドの約100案は**すべて全複利ブック（9枠・risk% 一律 0.5%）の上**で測られた。
   T036系は **RSI 3枠に実効3%/取引**を集中させる別構成なので、
   **同じ出口の軸でも効き幅が別物になりうる**（第22報「複利の増幅は 12〜121倍」）。

## 1. 2系統の違い（ここを混ぜない）

| | 全複利ブック（`ml/fxqual*`・XM） | **T036系**（`ml/fxoanda3`・**OANDA BT1**） |
|---|---|---|
| 枠 | 9枠すべて | **E04 = 7枠**（Carry と PB USDJPY を外す） |
| サイジング | `FxRiskMask=31`・`FxRiskPct=0.5`×倍率（全枠 risk%） | `FxRiskMask=7`＝**RSI 3枠だけ risk% 1.0**・`GlobalLotMult=3`・他は固定ロット×`Mult_*` |
| 枠別重み | すべて 1.0 | PB UJ 0.5 / RSI UJ 2 / RSI EU 4 / RSI GU 4 / Pair 4 / SCA UJ 4 / SCA GJ 4 |
| 測った窓 | OOS・IS・FULL | **OOS と 24か月窓3本のみ。IS は無い** |
| 最良 | 倍率2・cap90: OOS 3.849%・IS 7.099%（eqDD 62.07%） | cap50: **OOS 5.537%**（残高DD 44.91%・IS 未測定・eqDD 未測定） |

**この2つは同じ端末・同じ窓で一度も比べていない。**

## 2. 🔴 Codex が割った — T036 の利益は RSI GBPUSD 1枠から出ている

Codex に `ml/fxoanda3/results.csv` の枠別列を割らせた結果（S050 通期 OOS）:

| 枠 | 純益 | 全体（+9,188,767）に占める割合 |
|---|---:|---:|
| **RSI GBPUSD** | **+5,845,180** | **63.6%** |
| RSI USDJPY | +1,353,381 | 14.7% |
| RSI EURUSD | +906,540 | 9.9% |
| （RSI 3枠 計） | +8,105,101 | **88.2%** |
| Pair | −1,997 | — |
| SCA USDJPY | −82,923 | — |

**つまり 5.537%/月 は「RSI GBPUSD 1枠の OOS 成績に 3%/取引を賭けた数字」である。**
分散しているように見えて、実体は1枠への集中である。

⚠️ **この発見で設計を1か所変えた。** 当初は全複利ブックで最良だった
`T001`(RSI UJ) ＋ `T008`(RSI EU) の組を移植するつもりだったが、
**T036 で金を出しているのは GU なので、`T005`（`RsiTpMask_GU=2` / `RsiTpMult_GU=1.5`）を
単独で測る `O023` を主役に据えた。** EU は Codex の評価が「見込み低・OOS の equity DD がむしろ悪化」
なので本ラウンドからは外した。

## 3. 事前予想（走る前に書く。外れたら捨てる）

| run | 中身 | **Claude の予想** | **Codex の予想** |
|---|---|---|---|
| `O000` | S050 OOS 回帰 | 再現する（±0.5%）。見込み**中** | 5.537%・残高DD 44.91% と一致 |
| **`O001`** | **S050 IS** | **幾何 1.5〜3.0%/月。破綻も3割** | **3.8%**（レンジ 2.5〜4.8%） |
| `O023` | GU の TP | 効き幅 ±0.3pt・**見込み中** | 見込み**中**（IS 優先なら最も意味がある） |
| `O020` | UJ の TP | 効き幅 ±0.3pt・見込み中 | 見込み**中** |
| `O022` | GU＋UJ 合成 | 加算しない。符号は読めない | — |
| `O002` | S070 IS | 4%前後 | **4.5%**（S050 より高いが DD も高い） |
| `O003` | S070 通期OOS | 走り切る。**eqDD は 70% 超** | **6.2%**・完走するが DD は高い |
| `O011` | 全複利 倍率2・**cap90** | **ロスカットで打ち切られる**（維持率 111%）。見込み**低** | 失格の可能性 |
| `O011c` | 全複利 倍率2・cap50 | — | OOS 2.4〜3.1% / IS 4.0〜5.2% |
| `O010c` | 全複利 倍率1・cap50 | — | OOS 1.6〜2.0% / IS 2.8〜3.5% |

**Claude と Codex は「T036 の IS は 6% に届かない」で一致している。**
違いは幅で、Claude のほうが悲観的（破綻を織り込む）である。

> [!warning] **`O001` が 4% 未満なら、枠の質の探索より先に T036 を採用候補から外す**（Codex の判定規則を採用）。
> その場合、本ラウンドで残るのは「2系統を同じ端末で比べた表」だけになる。**それでも価値はある**が、
> **収益は1円も改善しない**。**そうなる公算のほうが大きいと考えている。**

## 4. 走らせる順（途中で止まっても価値の高い側から埋まる）

IS を先に取るのは、ユーザー方針が「トレードオフでは IS を優先」だから。

| # | run | 窓 | 目的 |
|---:|---|---|---|
| 1 | `O000` | OOS | **回帰**。fxoanda3 の 9,188,767円 を再現するか |
| 2 | **`O001`** | **IS** | ★本ラウンドの主役 |
| 3 | `O023` | IS | ★枠の質・稼ぎ頭 GU の出口 |
| 4 | `O020` | IS | 同・UJ |
| 5 | `O022` | IS | 合成（加算性を測る） |
| 6-8 | `O023`/`O020`/`O022` | OOS | 同じ3案の OOS |
| 9 | `O002` | IS | cap70 の IS |
| 10 | `O003` | OOS | cap70 の通期OOS（fxoanda3 で起動失敗した穴） |
| 11-12 | `O011c` | IS/OOS | **cap を揃えた**2系統比較（倍率2） |
| 13-14 | `O010c` | IS/OOS | 同（倍率1） |
| 15 | `O011` | OOS | cap90 は OANDA で建てられるのか |
| 16 | `O004` | IS | cap33 の IS（S050 が壊れたときの退避先） |
| 17-18 | `O021` | IS/OOS | SCA USDJPY レンジ幅下限の移植 |

## 5. 🔴 走らせる前に踏んだ地雷 — OANDA 端末の EA が古かった

| 場所 | 版 | サイズ |
|---|---|---|
| OANDA BT1 の `MQL5/Experts` | **2026-09-18 00:13** | 149,077 バイト |
| リポジトリ `experts/`（＝XM 端末と同一） | 2026-09-19 20:08 | 196,104 バイト |

**古い版は第12〜13ラウンドで足した `RsiTpMask_*` / `ScaRR_*` / `ScaOvsATR_*` / `ScaBE*` を持っていない。**
MT5 は知らない input を**黙って無視する**ので、そのまま走らせれば
`O020`/`O021`/`O022`/`O023` が `O000` と完全同値になり、
**「効かなかった」という結論に化けていた**（第16報で実際に踏んだ形）。

→ `chain.ps1` が **OANDA 側の `MetaEditor64.exe` で再コンパイルしてから**走らせる。
旧 `.ex5` は `MIX_EA_SIMVERIFY.20260918.ex5.bak` に退避する。
**XM 端末（`BAC624...`）には一切触らない**ので、並行セッションとバイナリを取り合わない。

### 回帰 `O000` の読み方（ここを曖昧にしない）

追加 input はすべて既定が no-op であることをソースで確認した
（`RsiTpMask_*=0` / `ScaRR_*=0.0` / `ScaFilMask=0` / `LotFloorMask=0` …）。
**`TagDealTriggers` と `PbDiagCounters` だけは `true` にしている**が、
前者は**注文コメント文字列**を変えるだけ（`RSI` → `RSI:RB`）、
後者は**カウンタと CSV 出力**だけで、どちらもロット・価格・SL/TP に触れないことを実装で確認した。

それでも diff には `LotRisk()` まわりの書き換えが含まれる。だから:

> **`O000` が一致しなかった場合、原因の候補は2つあり、この1本では切り分けられない。**
> **(a) EA の版が変わって挙動が変わった／(b) fxoanda3 の数字がそもそも再現しない。**
> **どちらであっても「fxoanda3 の数字と本ラウンドの数字を並べてはいけない」ことは確定する。**
> その場合は**止めずに走り切り、fxoanda4 内で閉じた比較だけ**を行う（`measure.py` の `check()` がそう実装してある）。

## 6. Codex が出した「まだ測っていない軸」（本ラウンドで消化しないもの）

Codex は T036 の上で再評価する価値がある軸を7件（A01〜A07）挙げたが、
**A01(UJ TP)・A03(GU TP) 以外はすべて自ら「見込み低」と書いている**。本ラウンドでは A01/A03 のみ測る。
残りは `O001` の結果を見てから判断する（T036 が IS で崩れるなら、そもそも掘る土俵ではない）。

**EA 改修が要る軸は2件。どちらも本ラウンドでは実装しない**（並行セッションと「EA を触らない」で合意済み）。

| ID | 中身 | Codex の見込み |
|---|---|---|
| A08 | 親 SCA のリバーサル boost を `ScaBoostMult_UJ/GJ` として input 化（現状は 6.0/2.0 のハードコード） | 中 |
| **A09** | **維持率ガード**: `MinMarginLevelPct` / `ResumeMarginLevelPct` で新規を止める（第1段）、`MarginEmergencyPct` / `MarginReduceFraction` で部分決済（第2段） | **高（利益ではなく実行可能性）** |

> [!important] **A09 は「`MarginCapPct` では OANDA の 100% ロスカットを守れない」という構造的な穴への答えである。**
> `MarginCapPct` は**発注時**に `使用証拠金 ≦ equity × cap%` を掛けるだけで、**建てた後の維持率を保証しない**。
> cap70 は建て直後 143% で、**逆行 30% 弱で切られる**。Codex の初期値案は
> 新規停止 180% / 再開 220% / 緊急削減 135%。**第18ラウンドの筆頭候補。**

## 7. 目標（月利6%）に対する現在地

> [!warning] ⚠️ **本節の「実行可能」という言い方は §8 で訂正した。**
> 下表の XM 端末の run は **XM の 20% ロスカットでしか検証されていない**ので、
> **OANDA（100%）で建玉を維持できるかは未検証**である。表の見出しもそう書き換えてある。

| 構成 | OOS 幾何月利 | IS 幾何月利 | 最大DD | OANDA での生存 |
|---|---:|---:|---:|---|
| 本番現行（RefCap=78,000・倍率1） | 0.39% | — | 5.2% | 実運用中 |
| 全複利 倍率1・cap90（XM） | 2.488% | 4.028% | 残高 23.4% / eq 34.6% | **未検証** |
| **全複利 倍率2・cap90（XM）＝発注制約を満たす最良** | **3.849%** | **7.099%** | 残高 46.3% / **eq 62.1%** | **未検証**（建て直後 ≈111%） |
| T036系 cap50（**OANDA 端末**） | **5.537%** | **未測定 ← 本ラウンド** | 残高 44.9% / eq 未測定 | 完走（建て直後 200%・最小維持率は本ラウンドで測る） |
| T036系 cap70（OANDA・24か月窓中央値 6.57%） | **通期未測定 ← 本ラウンド** | 未測定 ← 本ラウンド | 残高 W3 57.8% | W1〜W3 完走（建て直後 143%） |

**目標 6% に対し、発注制約を満たす最良は OOS 3.849%（1.56倍の不足）。
ただしそれが OANDA で生存するかは測れていない。**
T036系の 5.537% は**片窓だけの数字**で、本ラウンドが埋まるまで「最良」とは呼べない。
**Claude も Codex も、T036 の IS は 6% に届かないと予想している。**


---

## 8. 🔴 訂正 — 「実行可能」と書いてきたものは実行可能性を測っていなかった（2026-09-20 追記）

並行セッションの指摘で、**本書 §7 を含め、これまでの「実行可能な最良」という言い方が
不正確だった**ことが分かった。第23報以降ずっと引きずっていた誤りである。

### 何が間違っていたか

1. `MarginCapPct` は**発注の瞬間**に `使用証拠金 ≦ equity × cap%` を掛けるだけで、
   **建玉後の維持率を保証しない。**
2. **OANDA のロスカットは維持率 100%。XM は 20%。**
   cap90 の建て直後は維持率 ≈111% ＝ **11% の逆行で切られる。**
3. そして **全複利ブックの run はすべて XM 端末で走っている。**
   **MT5 のテスターは端末の口座設定を使う**ので、
   **モデル化されていたのは XM の 20% であって OANDA の 100% ではない。**

> **つまり「元本割れなし・完走した」は、
> 「XM の 20% に当たらなかった」という意味しか持っていなかった。**

| これまでの書き方 | 正しい書き方 |
|---|---|
| 「倍率2・cap90 は実行可能」 | **「発注時の証拠金制約は満たす。建玉後の維持率は未検証」** |
| 「確認済みの実行可能な最良 OOS 3.849%」 | **「発注制約を満たす最良。OANDA で生存するかは未検証」** |

**「危険である」とも書かない。** DD の谷と使用証拠金の山が同時に来たかが分からないので、
正確には **「危険かどうかが測れていない」**である。
**測れていないものを実行可能と書いたことが誤りだった。**

なお **この事実はプロジェクトが 2026-09-17 に既に記録していた**
（[oanda_stopout_level_20260917.md](oanda_stopout_level_20260917.md)・
[ALL_EA_performance.md](ALL_EA_performance.md) に「cap90 の建て直後 ≈111% → 11% の逆行で切られる」と明記）。
**索引にある結論を読まずに上書きしていた形である。**

### 既存のログでは測れなかった

- `*_cap.csv` の `event` 行は**発注時**の equity と使用証拠金しか持たず、
  しかも**発注時は cap の定義上どうしても `100/cap`% 以上**になる → **最小維持率を復元できない**
- `EquityLogFile` は名前に反して**実体が約定ログ**で、equity/証拠金の時系列ではない

### 入れた計装（`experts/MIX_EA_SIMVERIFY.mq5`・0 errors でコンパイル確認済み）

`TrackMarginLevel()` を `OnTick` の先頭（`MasterEnable` の直後）で呼ぶ。
`ACCOUNT_MARGIN > 0` のときだけ `ACCOUNT_MARGIN_LEVEL` を読み、
**最小値・その時刻・そのときの equity と使用証拠金**を保持する。
併せて **維持率が 300/200/150/100% を割っていたティック数**も数える（危険域の滞在時間）。
出力は `CapLogFile` に2行（`margin_level_min` / `margin_level_hist`）。
**`CapLogFile=""` なら1バイトも出ない。読むだけで売買には触れない。**
集計は `ml/fxoanda4/margin_level.py`。

> [!important] ⚠️ **ガードより先に計装を入れた**（並行セッションの指摘。これが肝だった）。
> `MinMarginLevelPct` のようなガードを先に入れると、**入れた時点で最小維持率が閾値で切り上がり、
> 「元の構成がどれだけ危なかったか」が永久に分からなくなる。**
> Codex の A09（ガード）は、**実測が出てから閾値を決める。**

### この訂正が本ラウンドに与える影響

🟢 **本ラウンドは OANDA 端末で走るので、100% のロスカットが実際にモデル化されている。**
XM 側の run と違い、**生存可否を直接測れる。** `O011`（全複利 倍率2・cap90 を OANDA で）は
まさにこれを測る run である。

💡 **維持率そのものは端末のロスカット水準に依存しない量**なので、
**XM 端末で測り直した run でも「100% を割ったか・いつ割ったか」は判定できる。**

> [!warning] ⚠️ **ただし「XM で測れば OANDA の生存可否が分かる」は半分しか正しくない**（並行セッションの訂正）。
>
> | 問い | XM の run で答えられるか |
> |---|---|
> | OANDA なら切られたか・いつか | ✅ **答えられる** |
> | OANDA での月利・DD はいくらか | ❌ **答えられない** |
>
> OANDA なら時刻 T で強制決済されるので、**T 以降の経路が別物になる。**
> XM の run は切られずに続いているため、**T 以降の損益・DD・月利はすべて反実仮想として無効**である。
> 実務的には **「割った構成は、成績を論じる前に候補から落ちる」**という使い方になる。
> **成績を知りたければ、OANDA 端末で（あるいはガードを入れて）測り直すしかない。**

### 🔴 計装そのものにも同じ型の穴があり、出す前に塞いだ

並行セッションの指摘。`TrackMarginLevel()` は `used<=0` / `ml<=0` で**黙って return** するので、
テスターで `ACCOUNT_MARGIN_LEVEL` が取れなければ
**`g_mlMin` も `g_mlLt100` も 0 のまま**になる。
そのまま出すと **「維持率 100% 割れは 0回＝安全」と読めてしまう。実際は「測れていない」。**

**これは本日4回繰り返した型そのもの**（量の名前を、満たしていない性質の含意つきで使う）なので、
**`g_mlSamples==0` のときは数値ではなく `NOT_MEASURED` を書き出す**ようにした。
`margin_level.py` 側も `NOT_MEASURED` を「安全」と数えずに別枠で報告する。

⚠️ `O000` の回帰にはこの計装ぶんの EA 差も乗る。**「触れないはず」で済ませず `O000` の一致で確かめる。**

### 副作用（申し送り済み）

リポジトリの `.mq5` と **XM 端末にデプロイ済みの `.mq5` が食い違った。**
`fxqual16` は DEPLOY_CHECK も再コンパイルも持たないので影響しないが、
**`sameAsRepo` チェックを持つ次の XM チェーンは `CHAIN_ABORT` する。**
次に XM 側を走らせるときは **XM 端末で再コンパイルすること**（計装も同時に載る）。


---

## 9. 実測が出た（並行セッション `ml/fxqual17`・XM 端末）— 従来の「最良」は OANDA では成立しない

> 本節の数字は**すべて並行セッションが XM 端末で測ったもの**（`ml/fxqual17/results.csv` と cap ログ）。
> **こちらの `ml/fxoanda4` はまだ1本も走っていない。**
> ⚠️ **月利は端末で 27% 違うので、下表の月利を OANDA の数字と同じ表に並べてはいけない。**
> **横断して並べてよいのは最小維持率だけ**（端末のロスカット水準に依存しない量だから）。

### 9.1 最小維持率 — 従来の「最良」は両窓とも 100% を割っている

| 構成（XM・全複利・cap90） | 窓 | 幾何月利 | 最小維持率 | 谷の日 | **R** | <100% ティック |
|---|---|---:|---:|---|---:|---:|
| 倍率3・**Carry 有** | OOS | 4.607% | **54.17%** | **2016-11-09（窓の初日）** | 0.488 | 109,297 |
| 倍率3・**Carry 有** | IS | 9.768% | **67.51%** | 2021-07-19 | 0.608 | 422,950 |
| 倍率3・**Carry 抜き** | OOS | 3.614% | **95.51%** | 2019-06-07 | 0.860 | 7,121 |
| 倍率3・**Carry 抜き** | IS | 6.407% | **99.09%** | — | 0.892 | **662** |
| 🟢 **倍率2・Carry 有** | OOS | **3.849%** | **101.47%** | 2018-12-17 | 0.913 | **0** |

🟢 **`倍率2・Carry 有・cap90` が、本プロジェクトで初めて維持率 100% を超えた**
（101.47%・100%割れ **0 ティック**）。**OANDA のロスカットを生き延びる構成が初めて実測された。**

🔴 **そして「倍率を下げる」は「Carry を外す」より安い:**

| 道 | 月利の代償 | 到達した最小維持率 | 生存 |
|---|---:|---:|---|
| Carry を外す（倍率3 のまま） | **−0.993pt**（4.607→3.614%） | 95.51% | ❌ |
| **倍率を1段下げる（Carry は残す）** | **−0.758pt**（4.607→3.849%） | **101.47%** | **✅** |

**安いほうが生き残る。「Carry を外す」という道は優先度が下がった。**
⚠️ **ただし 101.47% はロスカットまで 1.47pt しかない。**
スプレッド拡大・証拠金率変更・約定の滑りを考えると**実運用では余裕が無い。**
→ **話が「生存させる」から「実運用に耐える余裕を作る」に変わった。cap を絞る道がここで効く。**

🔴 **倍率3 は、Carry の有無にかかわらず両窓とも 100% を割っている。OANDA では切られる。**
**「実行可能な最良 4.608%」は、実行可能性を測らずに3回報告されていた。**

🟢 **`Carry` を外すと谷が「初日」から「窓の中ほど」へ移動した。**
これは水準の改善ではなく**原因の除去**である（Carry が無ければ初日に谷は立たない）。
**OOS の equity DD 73.62% も、維持率の谷も、初日の Carry 1枠が作っていた。**
これで第16ラウンドの「4案すべて equity DD が `73.6199%` と bit 単位まで同一」も説明がつく——
**初日に決まっていて、どの案もまだ枝分かれしていないから。**

⚠️ **ただし IS の谷は 2021-07-19（窓開始の約1か月後）で初日ではない。**
**「初日で決まる」は OOS に限る。**両窓で成り立つ話にしない。

⚠️ **Carry 抜きでも 100% は割っている**（95.51% / 99.09%）。ただし IS は 662 ティック（全体の
0.0007%）＝**あと一息で生き残る。cap を少し絞れば超える見込みが高い。**

🔴 **`IS 6.407%` を「目標達成」と読まないこと。判定窓は OOS（3.614%）である。**
IS はパラメータを合わせた窓なので、そこで 6% を超えるのは達成ではない。
**目標に対する現在地は OOS 3.614% で、6% には遠い。**

### 9.2 指標 R と、その使ってよい範囲

`MarginCapPct=c` は**発注の瞬間**にしか効かないので、**発注直後の維持率には床がある**:

    床 = 10000 / c   （cap33 303% / cap50 200% / cap70 143% / cap90 111%）
    **R ≡ 最小維持率 ÷ 床**

> [!warning] **R は外挿の道具ではない。**
> **床が保証するのは「建てた瞬間」だけ**で、谷の瞬間の使用証拠金は cap と無関係に決まっている。
> 実測: `M006 OOS` の谷は **equity 1,180,534 / 使用証拠金 1,236,016 ＝ equity の 104.7%**で、
> **cap90 をとっくに超えている。** つまり R が測っているのは「cap からの乖離」ではなく
> **「建てた後にどれだけ equity が落ちたか」**である。
>
> **R は倍率・枠構成・窓・端末に不変ではない。** 実測でも
> **窓をまたぐと 0.488→0.608 / 0.860→0.892**、**枠構成を変えると 0.488→0.860** と動く。
> 比べてよいのは **「同じブック・同じ窓で cap だけを振ったとき」**だけ。
> **本ラウンドは cap 33/50/70 が同一ブック・同一窓にあるので、そこで初めて測れる。**

### 9.3 🔴 計装は受動的ではなかった — cap 制約下では系統的に摂動する

`TrackMarginLevel()` は `AccountInfoDouble` を読むだけで売買ロジックに触れていないのに、
**損益が変わった。** 並行セッションが deal ログを1件ずつ突き合わせて機構を確定させた:

- **最初の食い違いより前の 100 建玉は、時刻・枠・ロット・価格まで完全一致**
- 食い違う注文は **発注時刻も価格も同じで、ロットだけが 1ステップ（0.01）ずれる**
- 取引数が 1179→1178 になった正体は、**最小ロット 0.01 の注文が cap でさらに削られて 0 になり、
  発注されなかった**こと

**FP ノイズなら最初から散らばるので、これは浮動小数の非決定性を否定する。テスターは決定的。**
**読み出しがテスター側の証拠金再計算を誘発し、cap 境界の注文の可否を反転させている。**

**適用範囲**: cap が効いていない構成（`cut=0`）では**完全に受動的**。**cap 制約下でのみ**摂動する。

**効き幅（pt を主、純益% を従）**:

| 窓 | 幾何月利の差 | （純益%） |
|---|---:|---:|
| OOS | **−0.0007pt** | −0.041% |
| IS | **+0.0693pt** | +3.877% |

⚠️ **純益% で見ると 95倍違って見えるが、それは採否を判断する単位ではない。**
**pt で見ると「OOS は無視できる / IS は 0.069pt」**——そして **0.069pt は `T001` の +0.011pt の6倍、
合成 `W005` の +0.106pt と同オーダー**である。
**摂動が候補の効き幅と同じ大きさだから、許容幅では吸収できない。**

> [!important] 🔴 **規約: 採否の比較は必ず同一バイナリ同士で行う。計装入りと計装なしの月利を混ぜない。**
> 橋を架けるために **`MarginTrackMode`** を用意した
> （**0＝計装OFF で旧バイナリと同一経路** / 1＝API / 2＝`equity ÷ 使用証拠金` で自前計算）。
> 本ラウンドの `O000off` がその橋で、**`fxoanda3` の 9,188,767円 を1円まで再現するか**を見る。

### 9.4 ラウンドを 18 → 25 run に増やした

| 追加 | 中身 | 理由 |
|---|---|---|
| `O000off` | 計装OFF で `S050` OOS | 旧バイナリとの橋 |
| `O020b`/`O020c` | UJ の TP 倍率 ×1.25 / ×1.75 | **`1.5` は全複利ブックでは尖り**（両隣が負）。台地か確かめる |
| `O012`/`O013` | 全複利・倍率3・Carry 抜き・cap70 / cap50 | `M006` の 99.09% を cap で超えられるか |
| **`O014`** | **全複利・倍率2・Carry 有・cap70** | **`M002` の 101.47%（余裕 1.47pt）を実運用に耐える水準まで上げられるか** |

**5本の道が1ラウンドに揃った**: 倍率を下げる（並行セッション）・Carry を外す（両方）・
**cap を絞る（同一ブック・同一窓で 33/50/70）**・
**最有力に余裕を作る（`O014` cap70 と `O011c` cap50）**・**倍率3 の救出（`O012`/`O013`）**。
**27 run。**

### 9.5 目標（月利6%）に対する現在地

| 構成 | OOS 幾何月利 | IS 幾何月利 | 最小維持率(OOS) | **OANDA で生存** |
|---|---:|---:|---:|---|
| 本番現行（RefCap=78,000・倍率1） | 0.39% | — | 未測定 | 稼働中 |
| 倍率3・Carry 有・cap90（従来の「最良」） | 4.607% | 9.768% | 54.2% | **❌** |
| 倍率3・Carry 抜き・cap90 | 3.614% | 6.407% | 95.5% | **❌** |
| 🟢 **倍率2・Carry 有・cap90** | **3.849%** | **7.099%** | **101.47%** | **✅（余裕 1.47pt）** |

🟢 **生き残る構成が初めて1つ見つかった: OOS 3.849% / IS 7.099%。**
🔴 **ただし目標 6%（OOS）には 1.56倍の不足。** そして **余裕が 1.47pt しかない。**
**「実行可能な最良 4.608%」は消えたが、代わりに「実行可能だと実測された 3.849%」が立った。**
**数字は下がったが、初めて意味のある数字になった。**


---

## 10. 🔴 §9 の訂正 — 「倍率2・Carry 有が初の生存構成」は OOS だけを見た誤りだった

`ml/fxqual17` の全16run が完了し、**IS が出た時点で結論が入れ替わった。**
§9 は `M002 OOS` しか出ていない時点の整理で、**そこに書いた「生き残る構成が初めて見つかった
（倍率2・Carry 有・OOS 3.849%）」と「Carry を外す道は優先度が下がった」は撤回する。**

### 10.1 両窓で見た生存表（XM・全複利・cap90）

| 構成 | OOS 維持率 | IS 維持率 | **両窓生存** | OOS 月利 | IS 月利 |
|---|---:|---:|---|---:|---:|
| 倍率1・Carry 有 | 118.33% ✅ | 105.85% ✅ | **✅** | 2.488% | 4.066% |
| 倍率1・Carry 抜き | 116.69% ✅ | 109.60% ✅ | ✅ | 1.915% | 2.406% |
| 倍率2・Carry 有 | 101.47% ✅ | **96.51% ❌** | **❌** | 3.849% | 7.033% |
| 🟢 **倍率2・Carry 抜き** | **105.13% ✅** | **102.25% ✅** | **✅** | **3.186%** | **4.506%** |
| 倍率3・Carry 有 | 54.17% ❌ | 67.51% ❌ | ❌ | 4.607% | 9.768% |
| 倍率3・Carry 抜き | 95.51% ❌ | 99.09% ❌ | ❌ | 3.614% | 6.407% |

### 10.2 何が入れ替わったか

**「倍率を下げる」か「Carry を外す」かの二択ではなく、倍率2 では両方が要る。**

- 倍率2・**Carry 有** → **IS 96.51%・5,729 ティック割れ。失格。**
- 倍率2・**Carry 抜き** → **両窓 ✅。これが両窓生存の最良で OOS 3.186% / IS 4.506%。**

> [!warning] 🔴 **教訓（今日3度目の同じ型）: 片窓だけで生存を宣言した。**
> `実行可能` を測らずに使った → `目標を超える` を窓を言わずに使った → **`生存` を片窓で使った。**
> **どれも「性質を主張する語を、その性質を確かめる前に使った」形である。**
> **生存は両窓で確かめる。** `CLAUDE.md` の「IS 窓と OOS 窓の両方を併記」は
> 損益だけでなく**維持率にも掛かる。**

### 10.3 実行可能な最良（現時点）

**倍率2・Carry 抜き・cap90 — OOS 3.186% / IS 4.506%・維持率 105.13% / 102.25%・両窓とも 100%割れゼロ。**
§9 に書いた 3.849% は取り消し。**−0.663pt の下方修正。**

### 10.4 本ラウンドの `O012`/`O014` の価値は**上がった**

**落ちているのは主に IS なので、IS から測るよう並べ替えた。**

| run | 中身 | 救出できたときの取り分 |
|---|---|---|
| **`O014`**（倍率2・Carry **有**・cap70） | IS 96.51% を cap で救えるか | **IS 7.033% / OOS 3.849%**＝両窓生存の最良より **+2.53pt / +0.66pt** |
| **`O012`**（倍率3・Carry 抜き・cap70） | IS 99.09%（**あと 0.91pt**）を救えるか | **IS 6.407% / OOS 3.614%**＝同 **+1.90pt / +0.43pt** |

**`O014` は「余裕を作る run」から「Carry を抱えたまま倍率2 を通せるかの run」に変わった。**
`O011c`/`O013`（cap50）は、cap70 で足りなかったときの退避先として 11〜14 番に置いた。

### 10.5 副産物 — 枠の質の唯一の候補 `W005` は余裕を持って生存する

| | OOS 月利 | OOS 維持率 | IS 月利 | IS 維持率 |
|---|---:|---:|---:|---:|
| 対照（倍率1・cap0） | 2.488% | 118.33% | 4.029% | 104.93% |
| **合成 `W005`**（`T001`＋`T008`） | **2.594%** | **120.25%** | **4.036%** | **105.22%** |

**両窓とも損益・維持率がともに改善。維持率まで含めて悪化なしの候補はこれが初めて。**
⚠️ ただし **`×1.5` が尖りかどうかの倍率掃引が未了**なので保留は継続（本ラウンドの `O020b`/`O020c` と、
並行セッションの `fxqual18` 候補）。
