# OANDA FX — 枠の質の第9ラウンド（第19報・`ml/fxqual9`・16run）

## 状態機械に残っていた最後の入口2軸。**このテーマを閉じるためのラウンド**である

> [!important] **これは改善探索ではない。**
> Codex の棚卸し（[codex_oafx_inventory_20260919.md](codex_oafx_inventory_20260919.md)）に、
> 第1〜7ラウンドの実測を全部渡したうえで「残っている合理的な作業」として挙がったのが
> **この2軸だけ**だった。事前査読でも同じ結論が繰り返されている:
>
> > この2軸を、上記の状態機械を正して測り終えた時点で、
> > **「FX 9枠の質改善」として実行価値のある未測定軸はゼロ**です。
> > 従って第9ラウンドは、改善探索ではなく、**PB 状態機械に残った最後の入口仮説と
> > Pair の遅延 entry 仮説を反証して、このテーマを閉じるラウンド**です。
>
> （[codex_oafx_round9_20260919.md](codex_oafx_round9_20260919.md)）

## 0. 何を測るか

| 軸 | 枠 | いまの実装 | 足すもの |
|---|---|---|---|
| **A. armed の寿命** | PB USDJPY / GBPJPY | `armedBuy/Sell` は `up`/`dn` が崩れるまで**無期限に残る**。100本前の押し目でも生きている | `PbArmMaxBars_UJ` / `_GJ`（0=無期限＝現行） |
| **B. Z の転換待ち** | Pair EURUSD/GBPUSD | `\|z\|>=entryZ` になった足で**その場で**両脚を建てる。乖離が拡大中でも建つ | `PairRequireZTurning`（false=現行） |

**A を選んだ根拠は第17報の leave-one-out。** PB 入口の律速は **slope → ADX → armed** の順で、
armed は PB USDJPY・IS で 126〜170バーを単独で落としていた
（[round3](oanda_fx_sleeve_quality_round3_20260919.md)）。**slope と ADX は両枠・両側とも掃き終えている**
（[round4](oanda_fx_sleeve_quality_round4_20260919.md) / [round6](oanda_fx_sleeve_quality_round6_20260919.md)）
ので、**armed だけが残っていた。**

## 1. 事前査読で実装バグが1件見つかった（測る前に直した）

当初案は `armed` の set と同じ行で齢を0に戻していた:

```cpp
if(up && lp<=fastema && qb) { S[i].armedBuy=true; S[i].armedBuyAge=0; }   // ← 当初案
```

> [!warning] **`lp<=fastema` が3バー続けば、齢は3回リセットされる。**
> これで測れるのは「armed されてから N バー」ではなく
> **「最後に押し目を付けてから N バー」**という**別の軸**である。
> しかも**第5ラウンドの「ずれる」問題を PB 側に持ち込む**——
> 期限切れになっても、押し目条件が続いていれば次の足で再 arm されるので、
> **入口が消えるのではなく後ろへずれるだけ**になる。

直した形は**齢の起点を押し目条件の立ち上がりに限定**し、
**`armed` の set/clear の5行は1文字も触らない**:

```cpp
// 既存の set/clear 5行はそのまま（無改変）
if(S[i].armMaxBars>0){                       // ← 0 ならブロックごと実行されない
   bool pbNowB=(up && lp<=fastema && qb);
   if(!up)                          S[i].armAgeB=-1;   // トレンドが壊れたらエピソード終了
   else if(pbNowB && !S[i].pbPrevB) S[i].armAgeB=0;    // 立ち上がりでだけリセット
   else if(S[i].armAgeB>=0)         S[i].armAgeB++;
   S[i].pbPrevB=pbNowB;
   if(S[i].armedBuy && (S[i].armAgeB<0 || S[i].armAgeB>S[i].armMaxBars)) S[i].armedBuy=false;
}
```

**set 側を立ち上がりのみにしなかった理由**: 発注時に `armedBuy=false` が入るので、
立ち上がりのみにすると**発注後に押し目が続いても再 arm されず、`amb=0` でも挙動が変わる。**
齢だけを立ち上がり起点にすれば、**`armMaxBars=0` での同値性はコード上自明**になる。

Pair 側も査読で2点直した:

| 指摘 | 直した形 |
|---|---|
| `PairRequireZTurning=false` でも `zPrev` を書くと既定値に副作用が出る | **false のときは1バイトも書かない** |
| `sd<=0` や `CopyClose` 失敗で z を作れなかったバーの後、古い z と比べて誤判定する | その経路で **`zPrevValid=false` に戻す** |

### 本番移植時に必要なこと（今回はやらない）

`armed` は GlobalVariable に永続化されている（v1.4・EA 1359行）が、**齢は永続化しない。**
ライブ再起動後は齢が −1 になるので、`armMaxBars>0` なら**復元された armed は落ちる。**
「古い arm を使わない」という趣旨には沿うが、本番に載せるなら
`StSnap`/`StSave`/`StRestore` に「arm されたバー時刻」を足して復元時に換算する必要がある。
**テスター／最適化では永続化自体が無効なので、今回の実測には一切影響しない。**

## 2. 案（8案・16run）

| 案 | 内容 |
|---|---|
| **A000** | 対照。新入力を全部既定値。W000 / Z000 / RV00 と1円まで一致すること |
| A001〜A005 | **PB USDJPY の armed の寿命 1 / 2 / 3 / 5 / 8 バー**（H4 なので 1バー=4時間） |
| A006 | PB GBPJPY の armed の寿命 3バー |
| A007 | **Pair: 乖離が縮小へ転じた足でしか建てない** |

対照は**現行（slope 1.2）の上**に取る。`ml/fxqual6` の slope 1.35〜1.40 の上に重ねる選択肢も
あったが、**別枠どうしの効果は両窓とも1円まで足し算**であることが3例で確認できている
（[round4](oanda_fx_sleeve_quality_round4_20260919.md) の W007・[round5](oanda_fx_sleeve_quality_round5_20260919.md) の X004）ので、後から合成できる。

UJ に5点・GJ に1点という配分は、LOO で **UJ の armed が 126〜170バー、GJ は 36〜58バー**と
UJ のほうが情報量が高いことによる。

## 3. 事前の予想（**測る前に書く**）

> [!warning] **両方とも期待していない。第5ラウンドの「ずれる」問題が、バー単位で当てはまる。**
>
> 第5ラウンドで、SCA の時間帯ゲートは「止めた発注が消える」のではなく
> **「後ろの足へずれる」**ことが分かり、切り直しの予測（+13,093）が実測（−6,817）と
> **符号ごと逆**になった。**判定条件は「そのゲートが日内で解除されるか」**だった。
>
> - **PB**: armed が期限切れになっても、押し目条件が**一度消えて再成立すれば新しい arm になる。**
>   トレンドが続いていれば押し目は何度も来るので、**入口が消えるのではなく後ろへずれる。**
> - **Pair**: 今日 turning しなくても、`|z|>=entryZ` が続いていれば**明日 turning した足で入る。**
>   一気に閾値内へ戻った取引だけが消える。
>
> つまり**どちらも「取引を減らす」より「取引をずらす」効果のほうが大きい**と予想する。

### 判定条件を1段細かくした（**A001 以降の結果が出る前に書く**）

並行セッションが `ScaFilRangeMin` を「日内で解除されないゲート」に分類して誤り、
訂正した際に出した一般化がここにも効く:

> **「解除されるか」は入力の性質ではなく、閾値が何と比較されているかで決まる。**
> `ScaFilRangeMin` の入力（レンジ幅）は9時に確定するが、
> 比較式は `dist / entry` で **`entry` はその時点の `ask`** なので、
> 価格がレンジから離れるほど比が増え、**初回ブレイクで止めても再ブレイクで通る。**

この物差しを本ラウンドの2軸に当てると、**2つは同じ「ずれる」ではない**:

| 軸 | 比較式 | 解除されるか |
|---|---|---|
| **PB の armed 寿命** | `armAgeB > armMaxBars` | **同一の押し目エピソード内では解除されない。** 齢は単調に増えるだけで、0 に戻るのは**次の押し目が立ち上がったとき**だけ（立ち上がり起点にしたので）。つまり**「次のエピソードまで待つ」** |
| **Pair の Z 転換** | `z < zPrev` | **毎バー解除されうる。** `z` も `zPrev` も毎バー動くので、翌足で縮みに転じればその足で入る |

**したがって予想を分ける:**

- **Pair（A007）は強くずれる。** 取引数はほとんど減らず、**入る足が1〜数本後ろにずれるだけ**だと予想する。
  損益差は小さく、符号は読めない。
- **PB（A001〜A006）は、ずれるがエピソード単位。** 同じ押し目では入り直せないので、
  **短い寿命ほど取引が実際に減る**はずである。ただし次の押し目で arm し直すので、
  **トレンドが続いているかぎり完全には消えない。**

**`shift.py` のずれ率で、この読みを直接確かめる。**
Pair のずれ率が高く、PB のずれ率が寿命を短くするほど下がれば、上の読みが当たったことになる。

### 効き幅の見積もり

| 軸 | 当方 | Codex |
|---|---|---|
| PB USDJPY の armed 寿命 | 月利 ±0.02pt | **−0.02〜+0.01pt** |
| PB GBPJPY の armed 寿命 | 月利 ±0.01pt | **−0.01〜+0.01pt** |
| Pair の Z 転換 | 月利 ±0.01pt | **−0.03〜+0.01pt** |

**両者とも「最頻の結果はゼロ近傍か悪化」で一致している。**

### 採否の基準（**先に書く**）

1. **勾配（1/2/3/5/8）が台地か単調でなければ棄却する。** PB USDJPY の OOS は **8 deal ＝ 4往復**
   しかない。`ml/fxqual6` の slope が「1.30〜1.50 の5点が両窓プラス」＝台地だったので
   採用候補になったのと**同じ基準**を当てないと、採用根拠にならない。
2. **正の一点が出ても採らない。** 第16報の V007（OOS の内訳だけで機構を選んで IS を壊した）と
   第7ラウンドの Z007（結果を見てから枠を選んだ）で、同じ失敗を2回している。
3. Pair は OOS 30脚 / IS 56脚しかない。`ml/fxqual2` の T001〜T007 が全部ほぼゼロだった。
   **±0.01pt 以内ならノイズとして扱う。**

## 4. 実測 — **PB は全点で両窓マイナス。Pair だけ両窓プラスだが桁が足りない**

### 回帰試験（EA を差し替えて再コンパイルした後）

| 窓 | 期待値（W000/Z000/RV00） | A000 | 判定 |
|---|---:|---:|---|
| OOS | +117,843 / 5.2719% / 1375 | **+117,843 / 5.2719% / 1375** | **一致** |
| IS | +255,445 / 7.3544% / 1595 | **+255,445 / 7.3544% / 1595** | **一致** |

**`PbArmMaxBars_UJ/_GJ`（0）と `PairRequireZTurning`（false）が既定値で1ビットも変えない**ことの実測。
並行セッションの `fxqualcfm` は**旧バイナリ**で走ったので、**これが唯一の独立確認**である。
恒等式（枠の和＝ブック純益）も全16run で未配賦 0。

### 損益・単利月利・最大DD（固定サイジング・50万円・**全 run 口座破綻なし**）

| 案 | 内容 | 窓 | 純益 | **Δ** | **単利月利** | 残高DD | equity DD | 取引 |
|---|---|---|---:|---:|---:|---:|---:|---:|
| **A000** | 対照 | OOS | +117,843 | — | **0.428%** | 5.27% | 7.04% | 1375 |
| | | IS | +255,445 | — | **0.852%** | 7.35% | 7.51% | 1595 |
| A001 | UJ 寿命 1バー | OOS | +113,909 | **−3,934** | 0.414% | 5.50% | 7.08% | 1361 |
| | | IS | +235,033 | **−20,412** | 0.783% | 7.26% | 7.51% | 1557 |
| A002 | UJ 2バー | OOS | +112,689 | **−5,154** | 0.410% | 5.52% | 7.30% | 1362 |
| | | IS | +229,562 | **−25,883** | 0.765% | 7.55% | 7.66% | 1563 |
| A003 | UJ 3バー | OOS | +114,096 | **−3,747** | 0.415% | 5.49% | 7.26% | 1364 |
| | | IS | +233,104 | **−22,341** | 0.777% | 7.55% | 7.93% | 1568 |
| A004 | UJ 5バー | OOS | +116,648 | **−1,195** | 0.424% | 5.78% | 7.54% | 1365 |
| | | IS | +235,040 | **−20,405** | 0.783% | 7.08% | 7.90% | 1570 |
| A005 | UJ 8バー | OOS | +111,293 | **−6,550** | 0.405% | 6.27% | 8.02% | 1371 |
| | | IS | +248,400 | **−7,045** | 0.828% | 7.08% | 7.80% | 1584 |
| A006 | GJ 3バー | OOS | +101,578 | **−16,265** | 0.369% | 5.26% | 7.03% | 1368 |
| | | IS | +244,055 | **−11,390** | 0.814% | 7.35% | 7.59% | 1587 |
| **A007** | **Pair Z 転換** | OOS | +118,228 | **+385** | **0.430%** | 5.30% | 7.23% | 1361 |
| | | IS | +256,260 | **+815** | **0.854%** | 7.30% | 7.52% | 1571 |

### 判定

| 軸 | 判定 |
|---|---|
| **PB の armed の寿命（A001〜A006）** | **棄却。** 1/2/3/5/8 バーの**5点すべてが両窓マイナス**で、台地も単調な改善もない。GBPJPY も両窓マイナス |
| **Pair の Z 転換（A007）** | **棄却。** 両窓プラスだが **月利 +0.0014pt（OOS）/ +0.0027pt（IS）**。採否基準3（±0.01pt 以内はノイズ）に照らして棄却 |

**採否の基準は測る前に §3 に書いてあり、そのとおりに適用した。**

### 枠別に見ると、効果は目的の枠に閉じていた

| 案 | 窓 | PB USDJPY | PB GBPJPY | Pair | その他6枠 |
|---|---|---:|---:|---:|---:|
| A001 | OOS | **−3,941** | +0 | +1 | +6 |
| | IS | **−20,478** | +0 | +2 | +64 |
| A006 | OOS | +0 | **−16,265** | +0 | +0 |
| | IS | +0 | **−11,390** | +0 | +0 |
| A007 | OOS | +0 | +0 | **+385** | +0 |
| | IS | +0 | +0 | **+815** | +0 |

**A006 と A007 は狙った枠以外がちょうど0円**＝副作用ゼロ。
A001 の RSI 2枠に出る ±10〜60円は、SL到達順序が変わることによる端数と見られる（月利では6桁下）。

## 5. 予想の照合 — **1つ当たり、1つ外れた**

### 当たり: 「両方ともゼロ近傍か悪化」

Codex の見積もり（PB −0.02〜+0.01pt / Pair −0.03〜+0.01pt）も当方の ±0.02pt も、
**実測（PB −0.014〜−0.059pt / Pair +0.0014〜+0.0027pt）の範囲に入っている。**

### 当たり: 「ずれ方が2軸で違う」（`shift.py` で直接確認）

§3 で**結果が出る前に**「比較式で決まる」として分けた読みが、そのまま出た:

| 案 | 窓 | 枠 | 対照 | 案 | 止められた | **ずれ** | **ずれ率** |
|---|---|---|---:|---:|---:|---:|---:|
| A001（PB・寿命1） | OOS | PB USDJPY | 18 | 4 | 15 | 0 | **0%** |
| | IS | PB USDJPY | 59 | 21 | 45 | 3 | **7%** |
| A004（PB・寿命5） | OOS | PB USDJPY | 18 | 8 | 10 | 0 | **0%** |
| | IS | PB USDJPY | 59 | 34 | 29 | 2 | **7%** |
| **A007（Pair）** | OOS | Pair | 30 | 16 | 26 | 12 | **46%** |
| | IS | Pair | 56 | 32 | 50 | 20 | **40%** |

**PB は 0〜7%、Pair は 40〜46%。** 予想した「PB は同一エピソード内で解除されない／
Pair は毎バー解除されうる」が、**ずれ率の桁の差としてそのまま出た。**

### 外れ: 「Pair は取引数がほとんど減らない」

**Pair は 30→16（−47%）/ 56→32（−43%）と、半分近くが消えた。**
ずれ率は高いのに取引数が大きく減るのは、
**`|z|>=entryZ` の状態が「縮みに転じないまま閾値の内側へ戻る」ことが多い**からである。
**ずれ率が高いこと**と**取引が残ること**は別だった——
止められた26件のうち12件は戻ってきたが、**14件は二度と条件を満たさなかった。**

> [!note] **教訓: ずれ率は「予測の外し幅」を説明するが、「取引が残るか」は説明しない。**
> 第5ラウンドの SCA（ずれ率89%・取引は 621→591 でほぼ残る）と、
> ここの Pair（ずれ率46%・取引は 30→16 で半減）は、**ずれ率と残存率が独立**である。

## 6. 副産物 — 第4ラウンドの「4往復」は **2倍の数え違い**だった

第4ラウンドと `project_status.md` 第17報つづきは、こう書いている:

> **OOS の PB USDJPY は 8 deal ＝ 4往復**で、**9往復から5往復**を落として残った4往復が出した数字である。
> IS は**22往復**で +12.6% と控えめ。

**数え直すと全部2倍だった**（`results.csv` の `trades` は deal 行数ではなく**建玉数**である——
`entry=0` の合計 1,375 が `trades` と一致することで確認した）:

| run | 窓 | **建玉（往復）** | deal 行数 | 枠純益 |
|---|---|---:|---:|---:|
| W000（対照・slope 1.2） | OOS | **18** | 36 | −1,812 |
| W001（slope 1.5） | OOS | **8** | 16 | +5,422 |
| W000 | IS | **59** | 118 | +22,267 |
| W001 | IS | **44** | 88 | +25,083 |

**正しくは「18往復から10往復を落として残った8往復」「IS は44往復」**である。
`8`（建玉数）を「8 deal」と呼び、そこから2で割って「4往復」としたのが誤りの中身。

> [!warning] **結論は変わらない。** 8往復は依然として少なく、採用根拠にはならない。
> `ml/fxqual6` で**台地（1.30〜1.50 の5点が両窓プラス）**を確認したから候補に残っているのであって、
> 往復数が倍でも「少数取引だから台地を要求する」という判断は正しかった。
> **ただし doc の数字は直す。**

## 7. 目標（月利6%）に対する現在地

| 項目 | OOS 月利 | IS 月利 |
|---|---:|---:|
| 目標 | **6.00%**（幾何・複利） | — |
| 対照（固定サイジング・単利） | 0.428% | 0.852% |
| **採用候補の合計**（V001 ＋ PB slope 1.40） | **0.463%** | **0.883%** |
| **本ラウンドの寄与** | **±0** | **±0** |

> [!warning] **9ラウンド・76案・150run。月利の前進は +0.035pt のまま。**
> 目標までの差は **+5.6pt/月**。**「枠ごとの質」というテーマでは埋まらない。**

## 8. このテーマを閉じる

Codex の棚卸し（[codex_oafx_round9_20260919.md](codex_oafx_round9_20260919.md)）の予告どおりになった:

> この2軸を測り終えた時点で、**「FX 9枠の質改善」として実行価値のある未測定軸はゼロ**です。

当方でも独立に抜けを探し、**Pair の回帰βヘッジ**を見つけたが、
**2026-06-27 に実測・棄却済み**だった（`pair_trade.md` ①・生差1:1 +12,340 に対し βヘッジ +2,870。
**勝率は上がるのに利益は激減**する）。**裏が取れた。**

**「枠ごとの質の改善」は、ここで打ち止めとする。**
残るのは**サイジング側**（並行セッションの `ml/fxqual10`＝SCA 2枠を risk% に入れてよいか）で、
そちらは**桁が2つ大きい**（SCA GBPJPY は複利下で FULL +115,992 → −1,826,948 と符号反転する）。

### 走らせる順番（記録）

`fxqual8` は 09:46 JST に完走したが、**その直後の空きは並行セッションの `ml/fxqualcfm`
（採用候補2件を FULL 窓と複利サイジングで確認・24run）が先に取った**（09:47:29）。
当方のチェック（09:47:17・テスターもロックも不在）から向こうの起動まで**12秒**で、
パッチ適用 → ハッシュ照合 → コミット → chain 起動の30秒の間に入られた形である。

**当方の chain は `/compile` を打つ前に待ちループへ入っていた**
（`.ex5` mtime 07:40:28・配備 `.mq5` は旧ハッシュのまま）。
`ml/fxmargin3/measure.lock` を見る条件を入れてあったので、
向こうの `measure.py` がロックを持っている間は `Quiet` にならなかった。

> [!warning] **ただしロック確認だけでは穴が残っていた。**
> 向こうの chain には「`FXQUALCFM_END` が無ければ60秒待って1度だけ再開する」retry がある。
> **`measure.py` が終わってから再開するまでの60秒はロックが空く**ので、
> そこで当方が起き出して `/compile` を打つと、**24run が2つのバイナリに割れる。**
> 待ち先を `FXQUAL8_END` から **`FXQUALCFM_END`** に変えて塞いだ。
> 起動済みだった chain（PID 852）は `/compile` を打つ前に停止させている。

**教訓: 「早い者勝ちで先に走らせる」と宣言するなら、宣言の前に準備を終えておく。**
パッチ・照合・コミットに30秒かかる手順を、宣言のあとから始めたのが原因である。

## 5. 運用上の事故（記録）

**09:10 頃、`fxqual8` の走行中にリポジトリの `MIX_EA_SIMVERIFY.mq5` を約1分間パッチしてしまった。**

複製に空撃ちするつもりで `sed` でパッチスクリプト内の対象パスを書き換えたが、
**Windows パスのバックスラッシュで `sed` の式が壊れて失敗**した。
`sed` の失敗を確認せずに出力を実行したため、**対象が元のまま＝リポジトリ本体**に当たった。

**実害は無かった**（`git checkout` で復旧・リポジトリと配備先のハッシュ一致・
`.ex5` の mtime 不変・`watchdog8.log` は START の1行のみで再起動ゼロ・
`measure.log` に欠落なし）。**`chain.ps1` が死ななかったので watchdog が起きなかっただけ**である。
窓は約1分、watchdog のポーリングは3分間隔だった。

再発防止として `patch_ea.py` に対象パスの引数を足し、
**複製への空撃ちは引数で明示的に指定する**形にした。パス書き換えの `sed` は使わない。

その後、正しく複製へ空撃ちして13箇所すべてが一意に一致することを確認し、
**複製を `MIX_EA_ARM9TEST.mq5` という別名で MetaEditor にかけて
`Result: 0 errors, 0 warnings` を取った**（配備中の `.ex5` の mtime は不変）。
