# フォワードテスト改善案 2026-09-05

対象期間: 前回改善案レポート（2026-08-29）以降＝2026-08-29〜09-05（実質5営業日 8/31-9/4）。
入力: [2026-09-05_weekly.md](2026-09-05_weekly.md)、mixlog CSV（3口座・7-9月分）、`launcher_log.txt`、`health_log.md`、MT5端末ジャーナル(`logs/*.log`、UTF-16)、Windowsイベントログ（Id 1074・読み取り専用）、レジストリ`Winlogon\AutoAdminLogon`（今回はサンドボックス制限なく直接確認可）、`ops/cross_broker_ledger.py` v2実行結果、EA-MT5-Watchdogタスク構成（`Get-ScheduledTask`）、`mt5_backtester`リポジトリのgit log・EAソース（`experts/MIX_EA_OANDA.mq5`）・端末側コンパイル済み`.ex5`のタイムスタンプ突合。

## 1. 要約

今週最大の成果は、週次レポートが🚨2として要確認扱いにした「SCA GBPJPYリバーサルBoostが0.06lotで既知値0.03の2倍」という疑問を、EA異常ではなく**2026-08-11にユーザー承認済みの`Boost_Mult 4.0→6.0`変更（コミット`751ad8e`）が、2026-08-22 19:31の3端末同時EA再デプロイで初めて本番に反映された結果**と特定できたこと。0.01lot×6.0=0.06で完全に整合し、🚨2はクローズとする（F-1）。もう一つの重要な進捗は、5週間「検証機会なし」と記録し続けてきた**S-11（Windows更新後の自動サインイン未設定）に、ついに実地検証の機会が訪れたこと**——8/31 03:30/03:36 JSTのWindows Update計画再起動（Id 1074）後、3端末とも07:33〜08:05 JST（約4時間後）まで復旧しなかった。`AutoAdminLogon`は今回直接確認でき、依然`0`のまま。幸い月曜最初のシグナル発生（10:15 JST）より前に復旧できたため実害はゼロだったが、**リスクは「未検証」から「実証済み・未解消」に格上げすべき**（F-2）。B-3台帳（XM執行コスト劣後）はN=26に到達し累計-1,249円（平均-48.0円/pair）、是正後(POST_S4)区間の劣後幅は拡大傾向が継続（B-6は着手判断待ち）。好材料として、health_log.mdの欠落は2週連続でゼロとなり、C-3をクローズ候補に格上げする。戦略レベルでは、SCA USDJPYが累計20件（10+10）に到達したが、SCA GBPJPYと同様08-22の一斉EA再デプロイによる母集団分断の懸念があるため判断は保留する。

## 2. 観測された事実

### F-1（新規・解決・🚨2クローズ）: SCA GBPJPYブースト0.06lotは異常ではなく、承認済み変更の初回本番反映と判明

週次レポート🚨2は、8/31のSCA GBPJPYリバーサルBoost取引が両口座とも0.06lotで、直近既知値(0.03lot)の2倍だった点を要確認としていた。EAソース（`experts/MIX_EA_OANDA.mq5:383`）を確認したところ、現在の値は`x.scaBoostMult=6.0`。gitログで変遷を追うと：

```
2026-08-08 12:13  scaBoostMult=2.0→3.0（新戦略探索ラウンド2で採用）
2026-08-11 13:24  scaBoostMult=3.0→4.0（Codex500案残余336案検証で採用）
2026-08-11 18:50  scaBoostMult=4.0→6.0（Codex500案ラウンド3・ユーザー承認、コミット751ad8e）
```

一方、3端末の実際にロードされている`.ex5`（コンパイル済みEA）のタイムスタンプは**3端末とも2026-08-22 19:31〜19:35**で揃っており、これが直近の本番デプロイ日。つまり8/11に承認・コミットされた`6.0`への変更は、**8/22の一斉再デプロイで初めて本番のEAバイナリに反映された**。8/21観測の0.03lot（Boost_Mult=3.0相当）はこの再デプロイ以前の古いバイナリでの挙動であり、8/31観測の0.06lot（=0.01×6.0）は再デプロイ後の**設計どおりの挙動**。ロット差の原因は「EAの不具合」でも「設定ファイルの不一致」でもなく、**「ソースが承認・コミットされた日」と「実際に端末へデプロイされた日」に約11日のラグがあった**ことによる一時的な見かけ上の矛盾。🚨2はクローズする。

**運用上の示唆**: このルーチンが参照する「既知値」（週次レポートの過去記述）は、ソース承認日ではなく本番デプロイ日を基準に更新する必要がある。次回以降、`.ex5`のタイムスタンプと`git log`を突合する手順を先に踏むことで、同種の誤検知を避けられる。

### F-2（新規・重要・S-11初の実地検証）: Windows Update計画再起動後、約4時間の全端末停止を確認——リスクは依然未解消

Windowsイベントログ（Id 1074）で対象期間を確認したところ、**8/31 03:30:07（MoUsoCoreWorker）および03:36:29（TrustedInstaller）に計画再起動が発生**していた——これはS-11が5週連続「検証機会なし」としてきた1074イベントの、今回初めての発生。

`launcher_log.txt`を確認すると、3端末のPROBLEM DETECTED（初回検知）は以下：

```
2026-08-31 07:33:32  [OANDA_FX]  PROBLEM DETECTED → 07:34:03 RECOVERED
2026-08-31 07:34:03  [OANDA_CFD] PROBLEM DETECTED → 07:34:52 RECOVERED
2026-08-31 07:34:52  [XM]        PROBLEM DETECTED → 07:35:38 RECOVERED
```

再起動（03:30/03:36）から最初のwatchdog検知（07:33台）まで**約4時間の空白**があり、この間3端末とも停止していたと推定される。現行の`EA-MT5-Watchdog`タスクは今回`Get-ScheduledTask`で直接確認したところ**`LogonType=Interactive`のまま**（S-9で提案した構成監視も`ops/watchdog_lib.ps1`に未実装のまま——`LogonType`/`RunLevel`を検索したが該当コードなし）。レジストリ`HKLM:\...\Winlogon\AutoAdminLogon`も今回は制限なく直接確認でき、**値は`0`のまま**（過去4回の確認と同じ）。

Windows Securityログの4624（ログオン成功）イベントは監査設定が無効なため記録がなく、07:33台に何が起動トリガーになったか（誰かの物理/リモートログオン、または別経路）は直接特定できなかったが、`LogonType=Interactive`のタスクが約4時間発火しなかった事実と整合する。**幸い月曜最初の新規シグナル（SCA USDJPY/GBPJPY 10:15-10:30 JST）は復旧後だったため実害はゼロ**だったが、これは08-14型（43時間）と同根の脆弱性が形を変えて再現したもの。5週間「検証機会なし」としてきたS-11は、**「未検証」から「実証済み・未解消」に格上げする**（S-11参照）。

### F-3（継続）: B-3台帳（クロスブローカー執行コスト）がN=26に到達、劣後傾向は継続

`ops/cross_broker_ledger.py`を2026年7-9月分mixlog全9ファイル（XM3・OANDA FX3・OANDA CFD3）で実行：

```
XM signals=42  OANDA signals=42  matched pairs=38  in cost stats N=26
coverage=90%  excluded: TIMEDIFF=11  S4=2
cumulative XM-OANDA diff = -1,249 JPY (mean -48.0 JPY/pair, N=26)
  PRE_S4    N=6   diff=-172 JPY (mean -28.7円/pair)
  S4_PERIOD N=1   diff=-53  JPY
  POST_S4   N=19  diff=-1,024 JPY (mean -53.9円/pair)
```

前回（08-29時点、N=22、累計-1,011円）からN=26・累計-1,249円に増加。POST_S4区間の平均劣後幅（-53.9円/pair）は前回（-52.4円/pair）からさらにわずかに拡大しており、依然「是正後の方が劣後幅が大きい」という傾向が続いている。UNMATCHEDは新規に9/1 09:15（XM単独）・9/1 09:45（OANDA単独）が加わり計8件、S4-DEDUPは3件（すべて07月）で変化なし。LOT_MISMATCHは今回もゼロ。B-6（要因深掘り）の着手判断は引き続きユーザー待ち。

### F-4（継続・改善確定）: health_log.mdの欠落が2週連続でゼロ

前週（08-29改善案）でC-3として「2週連続ゼロならクローズ候補」としていたが、今週（8/31-9/4）も5営業日×3回＝15回すべて記帳されており欠落ゼロを確認。2週連続達成につき、**C-3をクローズ候補とする**（下記backlog参照）。

### F-5（新規・軽微）: 9/2 04:05 JSTにOANDA FX単独でプロセス消失、16秒で自動復旧

```
2026-09-02 04:05:02  [OANDA_FX] PROBLEM DETECTED - terminal process not running. Recovering...
2026-09-02 04:05:18  [OANDA_FX] RECOVERED - EA attached and confirmed
```

OANDA FX端末ジャーナル（`logs/20260901.log`・`20260902.log`）を確認したところ、9/1 19:34:10（最終ログ行）以降、次の起動ログ（9/2 04:05:04 "successfully initialized from start config"）まで記録がなく、正常終了のシグネチャ（LiveUpdateの`start .../update`等）も見当たらない。CFD・XM側は同時間帯とも無関係に継続稼働しており（health_log 09-02 08:05時点で両口座とも「当日再起動なし」）、**この端末単独の原因不明な瞬断**。ただし今回はwatchdogの通常30分サイクルの1回で検知・復旧に成功しており（PROBLEM→RECOVERED 16秒）、8/26のLiveUpdate衝突型（4回中3回失敗・約37分）のような複合失敗パターンには発展しなかった。実害なし・単発のため様子見とする（C-5参照）。

### F-6（継続・情報のみ・改善案化しない）: 🚨1（CFD口座資金移動）は今週も状態変化なし

週次レポート🚨1で報告されたOANDA CFD→FXの資金振替（95,434円・8/29）は今週も継続しており、CFD口座はゼロ稼働（PB GOLD・SCA GOLDの2枠がテスト対象外）のまま。これは本ルーチンの権限外（読み取り専用・EA外の資金配分判断）であり、ユーザー確認待ちの事項として記録するに留め、改善案としては提案しない。

## 3. 改善案バックログ

**優先度: S=機能バグ級 / A=執行品質 / B=検証価値あり / C=様子見**

### S-11（継続・5週ぶりに実地検証・「未検証」から「実証済み・未解消」へ格上げ）: Windows「更新後の自動サインイン」設定

- **仮説:** `EA-MT5-Watchdog`タスクが`LogonType=Interactive`のままである限り、Windows Updateの計画再起動後に対話ログオンが発生するまでタスクが一切発火せず、EAが無防備な状態が続く。
- **根拠:** F-2。8/31 03:30/03:36の計画再起動後、3端末とも約4時間（07:33台まで）復旧しなかった。`AutoAdminLogon=0`（今回直接確認）、タスクの`LogonType=Interactive`も現行のまま（`Get-ScheduledTask`で確認）。今回は市場の閑散時間帯だったため実害はゼロだったが、再起動タイミングが取引時間帯にずれれば08-14型（43時間相当ではないにせよ数時間のシグナル取りこぼし）が再現しうる。
- **検証方法:** 引き続き運用設定変更の問題であり、バックテスト検証の対象ではない。`AutoAdminLogon`設定またはタスクトリガーの`S4U`/`AtStartup`化（過去に一度試みて`MDI create failed`等で撤回済み・S-5参照）のいずれかで対話ログオン非依存化を図り、次回の1074イベント発生時に復旧までの時間を実測する。
- **期待効果:** 再起動から復旧までの無防備時間を、現状の「ログオンが発生するまで不定」から「30分以内（watchdogの通常サイクル）」に短縮。

### C-3（継続→クローズ候補）: A-2 health_log検知の可視化不足

F-4のとおり2週連続（08-24〜08-28、08-31〜09-04）で欠落ゼロを確認した。当初「検知はしているが伝わらない」という懸念自体、直近2週は発生条件（欠落）が起きていないため検証対象がない状態。**このバックログ項目はクローズとする**（再発時は新規項目として再起票）。

### C-5（新規・様子見）: OANDA FX単独の原因不明な瞬断（9/2 04:05）

- **仮説:** F-5で観測した9/2 04:05の単発プロセス消失は、他端末と無関係かつジャーナルに終了シグネチャがないため、原因は現時点で特定不能（OS側のリソース回収、ネットワーク瞬断、その他）。
- **根拠:** F-5。実害なし・16秒で自動復旧・単発のみで再現性不明。
- **検証方法:** 次回以降も同型（単一端末・原因不明・ジャーナルに終了記録なし）の瞬断が発生するか観察を継続し、2回以上再現した場合のみ深掘りする。
- **期待効果:** 低（今回は監視体制が正常に機能し実害ゼロだったため優先度は低いが、記録として残す）。

### B-6（継続・変更なし）: XM執行コスト劣後（B-3台帳）の要因深掘り

前回（08-29）から状況変化なし。F-3のとおりN=26（累計-1,249円）に増加し、POST_S4区間の劣後幅（-53.9円/pair）もわずかに拡大継続。着手判断はユーザー待ち。

### B-4（継続・変更なし）: SCA GBPJPYのレンジ境界感度分析（±2/5/10pips）

前回から状況変化なし。今週の新規UNMATCHED（9/1分2件）はB-3台帳経由で確認されたが、実損を伴う新事例はなし。着手判断はユーザー待ち。

### S-12（継続・今週は再発せず・良好事例として記録）: watchdog再起動サイクルとMT5 LiveUpdateの衝突

前回（08-26）観測した「LiveUpdate衝突で4回中3回の再起動試行が失敗し約37分停止」というパターンについて、今週8/31 07:33-08:05のLiveUpdate関連の再起動（build 6090→6140、週次レポート既報）を確認したが、**3端末とも1〜2サイクル・30〜90秒以内に復旧しており、08-26型の複合失敗は再現しなかった**。S-12の必要性を裏付ける新規事例はないが、リスク自体は解消されていない（実装は依然未着手）ため優先度は維持しつつ、「良好事例」として記録する。

**戦略提案なし** — SCA USDJPYが累計20件（10+10）に到達したが、SCA GBPJPYと同様08-22の一斉EA再デプロイ（F-1参照：v1.8 `Break_Buffer_ATRd 0.05→0.10`等の反映時期）による母集団分断の懸念があり、一貫標本としての判断は時期尚早。他の枠は全て前回比で観察継続レベルの増分に留まる。

## 4. サンプルサイズの現状

累計決済数（全口座合計、2026-07-06以降。`2026-09-05_weekly.md` §3の確定値＝mixlog集計を採用）:

| 枠 | 累計決済数 | 累計損益(円) | 統計判断の目安 |
|---|---|---|---|
| SCA GBPJPY | 45（21+24） | -311（+1,133-1,444） | N≥30到達だが08-10/08-22の複数回のEA差替え（Boost倍率変遷含む）で母集団分断のため一貫標本としては未達・判断保留継続 |
| SCA USDJPY | 20（10+10） | -1,593（-708-885） | **今週N=20到達**。ただしSCA GBPJPYと同型の母集団分断懸念（v1.8パラメータ変更の反映時期）があり判断保留 |
| SCA GOLD | 15（7+8） | -27,877（-13,201-14,676） | 観察継続（<20・CFD口座ゼロ稼働により新規サンプル増加が当面停止＝🚨1参照） |
| RSI EURUSD | 11（6+5） | -4,440 | 観察継続（<20） |
| RSI USDJPY | 7 | -3,913 | 観察継続（<10）。7戦全敗だが単一レジーム・少サンプルにつき戦略判断は保留継続 |
| PairTrade | 4 | +3,731 | 観察継続（<10） |
| VBO USDJPY | 6 | -1,479 | 観察継続（<10・`En_VBO=false`で現在無効） |
| RSI GBPUSD | 3（2+1） | -81 | 観察継続（<10）。今週XM初約定で母集団に加わったばかり |
| PB USDJPY | 1 | -1,750 | 観察継続（<10） |
| PB GOLD | 1 | -11,365 | 観察継続（<10・CFD口座ゼロ稼働により新規サンプル増加が停止） |
| PB GBPJPY / Carry / ETH（決済ベース） / BTC_FUND / BfxRev | 0（ETHは保有中1本・含み+3,066が別枠であり未決済） | — | 未発火・判定不能継続（Carryは10週超連続0取引・ヒステリシス採用後は年1-3回が正常のため異常ではない） |

いずれの枠も戦略レベルでの改善提案を出すには時期尚早（この原則は今週も維持）。加えて、CFD口座ゼロ稼働が続く限りGOLD系2枠（PB GOLD・SCA GOLD）のサンプル増加は事実上停止している点に留意。

## 5. 次週の観察ポイント

1. **S-11の対応方針** — 今回初めて実地でリスクが顕在化（4時間の無防備ギャップ）。次回1074イベント発生時に同様の遅延が起きるか、対策実装の有無を確認
2. **B-6の着手判断** — B-3台帳がN=26に増加、POST_S4劣後幅がさらに拡大するか
3. **C-5（9/2 FX単独瞬断）の再発有無** — 2回以上再現したら深掘り対象に格上げ
4. **🚨1（CFD口座資金移動）の状態** — CFD口座がゼロ残高のまま継続するか、ユーザーの意思確認結果
5. **SCA USDJPYの母集団分断整理** — N=20到達を機に、08-10/08-22の複数デプロイ時期で区分した分析が可能か検討
6. **health_log.mdの欠落ゼロが3週連続で続くか**（F-4・C-3はクローズ済みだが再発監視は継続）

---

*本レポートはClaude（自動巡回）が読み取り専用で作成。EA・設定・端末への操作、発注・決済は一切行っていません。改善案は提案のみであり、採否はバックテスト検証（every_tick・IS/OOSゲート）を経てユーザーが判断します。*
