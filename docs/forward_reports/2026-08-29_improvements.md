# フォワードテスト改善案 2026-08-29

対象期間: 前回改善案レポート（2026-08-22）以降＝2026-08-22〜08-29（実質5営業日 8/24-8/28）。
入力: [2026-08-29_weekly.md](2026-08-29_weekly.md)、mixlog CSV（3口座・7-8月分）、`launcher_log.txt`、`health_log.md`、MT5端末ジャーナル(`logs/*.log`)、Windowsイベントログ（Id 1074・読み取り専用）、`ops/cross_broker_ledger.py` v2実行結果、レジストリ確認は本ルーチンのサンドボックス制限により今回未実施（詳細は F-6）。

## 1. 要約

今週は開始以来最大の下げ幅（equity -15,817円）となったが、内訳は既知のA-1（GOLD過大リスク・フォワードテスト中は現状維持と決定済み）がリバーサルBoostの両ブローカー同時SLヒットという形で実際に顕在化したものであり、EA・ロジック・執行に異常はない。運用面では新種の障害パターンが1件見つかった——8/26 06:35〜07:12の約37分、OANDA FX・CFD端末で`terminal64.exe`の再起動のたびにMT5のLiveUpdate自己更新機構が`terminal64.exe`のコピーで衝突（ファイル使用中エラー32）し、3/4回の復旧試行が失敗する新パターンが確認された。過去の同種停止（08-02のbuild自動更新、08-12・08-15の原因不明の瞬断）とはジャーナル上のシグネチャが異なり、今回が初observed。これに対し**S-12**を新規提案する。もう一つの重要な進捗は、クロスブローカー執行コスト台帳（B-3）がN=22に到達し、当ルーチンで定めた判定ライン（N≥20）を初めて超えたこと——累計XM-OANDA差は-1,011円（平均-46.0円/ペア）で、是正（S-4）後のPOST_S4区間だけを見ると平均-52.4円/ペアとむしろ是正前(-28.7円/ペア)より拡大しており、単なる過渡的ノイズでは説明しづらい。これを踏まえ**B-6**（XM執行コスト劣後の要因深掘り）を新規提案する。好材料として、前週まで2〜3営業日欠落していた`health_log.md`が今週は5営業日×3回すべて記帳され、欠落ゼロだった（F-4のC-3懸念は改善方向）。戦略レベルでは新規の判断材料なし。

## 2. 観測された事実

### F-1（継続・情報）: GOLD SCAリバーサルBoostの両ブローカー同時SLヒットがA-1を再確認

8/27 12:45 IN→20:00/20:01 OUTで、OANDA CFD -10,704円・XM -10,694円とほぼ同額の損切りが発生（週次レポート⑤-1で既報）。ドリフト符号・Boost倍率(0.02lot)とも設計どおりに作動しており、EA異常ではない。これは2026-07-25にユーザー決定済みのA-1（GOLD最小ロットでもRefCap比リスクが設計の5.7倍・フォワードテスト中は現状維持）が実際に発現した2例目（1例目は07-25のPB GOLD -11,365円）。新規提案は行わない（決定済み事項の再確認のみ）。

### F-2（新規・S級）: watchdog再起動時にMT5 LiveUpdateがterminal64.exeのコピーで衝突し、復旧が3/4回失敗する新パターンを確認

`launcher_log.txt`とOANDA FX端末ジャーナル(`logs/20260826.log`)を突合した結果、8/26 06:35〜07:12の間に以下の失敗連鎖が確認された：

```
06:35:04.268  LiveUpdate  start terminal64.exe /update ...
06:40:19.229  LiveUpdate  failed to create copy terminal64.exe -> ...\liveupdate\terminal64.exe [32]
06:40:19.409  LiveUpdate  start terminal64.exe /update ...  (再試行)
07:05:18.625  LiveUpdate  failed to create copy ... [32]
07:05:18.810  LiveUpdate  start terminal64.exe /update ...  (再試行)
07:10:19.833  LiveUpdate  failed to create copy ... [32]
07:10:19.998  LiveUpdate  start terminal64.exe /update ...  (再試行)
→ 07:12:49 RECOVERED（4回目の起動でLiveUpdateが介入せず正常起動）
```

`launcher_log.txt`側では06:35・06:45(ESCALATION)・07:05・07:10の4回のwatchdog再起動試行がこれに対応し、OANDA_CFDも06:45に同時多発（07:12:49に自己回復確認、明示的なRECOVERED行はなし）。エラー[32]はファイル共有違反（使用中）であり、watchdogが再起動サイクル（5分間隔）ごとに新しいterminal64.exeプロセスを起動する一方、直前のLiveUpdateプロセスまたは終了しきっていない旧プロセスがコピー先ファイルをロックしたまま次の起動と衝突している可能性が高い。

**過去の類似停止との区別**: 08-02(build 5836→6090自動更新後にEA脱落)・08-12(08:35、原因不明・48秒で自己回復)・08-15(43時間、ログオン依存問題)のジャーナルを同条件で検索したが、いずれも`LiveUpdate`シグネチャは**出現しなかった**（`logs/20260812.log`・`logs/20260815.log`とも"LiveUpdate"の一致ゼロ）。08-26のみに現れた新パターンであり、既存のS-8（更新後EA未アタッチの検知）とは異なる故障モード。実害（欠測シグナル）は確認されず、XM端末は無関係だった。

### F-3（新規・重要な節目）: クロスブローカー執行コスト台帳（B-3）がN=22でN≥20判定ラインを初めて突破

`ops/cross_broker_ledger.py`を2026年7-8月分mixlog全6ファイルで実行（`--s4-start "2026-07-20 18:07" --s4-end "2026-07-25 17:12"`）。

```
XM signals=36  OANDA signals=36  matched pairs=33  in cost stats N=22
coverage=92%  excluded: TIMEDIFF=10  S4=2
cumulative XM-OANDA diff = -1,011 JPY (mean -46.0 JPY/pair, N=22)
  PRE_S4    N=6   diff=-172 JPY (mean -28.7円/pair)
  S4_PERIOD N=1   diff=-53  JPY
  POST_S4   N=15  diff=-786 JPY (mean -52.4円/pair)
```

前回（08-22時点、N=19、累計-946円）からN=22・累計-1,011円に増加。**このルーチンで定めた「N≥20到達で採否判断」のラインを今回初めて超えた**。着目点は、S-4是正（07-25・XM二重ロード是正）後のPOST_S4区間の方が是正前(PRE_S4)よりも1ペアあたりの劣後幅が大きい（-52.4円 vs -28.7円）こと——是正がXM側の執行劣後そのものを縮小させたわけではないことを示唆する。全3区間で符号が一貫してマイナス（XM劣後）であり、LOT_MISMATCHは今回もゼロ（ロット差由来ではない）。UNMATCHEDは新規に07-24 10:30(XM単独)が加わり計6件（すべてGBPJPY・B-4の対象と重複）。

### F-4（継続・改善）: health_log.mdの平日欠落が今週はゼロに回復

前週（08-22改善案）でF-4として指摘した「8/19-21の3営業日欠落」に対し、今週（8/24-8/28）は5営業日すべてで08:06・12:06・18:06の3回記帳が欠落なく確認された。原因（記帳ルーチンの不調か記入漏れか）は特定されないままだが、症状としては解消した。C-3（検知はしているが可視化されない問題）は実害面では優先度を維持する必要は薄いが、**1週間の観測だけでは再発しないと断定できない**ため、来週も継続監視する。

### F-5（新規・軽微）: result_log.mdのXM有効証拠金記録に一過性の計算漏れ

週次レポート⑤-3で既報のとおり、8/28 12:06・18:06の2回のみ、XM行の「有効証拠金」がクレジット65,000円を含まない値（48,922／48,988）で記録されていた。read-only APIで直接確認した限りcredit実減額の形跡はなく、記帳スクリプト側の一過性の計算漏れと判断される。資金上の実害なし。

### F-6（継続・未検証）: S-11（AutoAdminLogon）の状態確認が今回はサンドボック制限で直接確認不可、かつ検証機会（1074イベント）も今週は発生せず

今回のルーチン実行環境では`HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Winlogon`のレジストリ直接参照が権限ポリシーによりブロックされ、`AutoAdminLogon`の値を実測できなかった（過去4週は`0`のまま確認済み）。一方、Windowsイベントログ（Id 1074・過去8日）を確認した結果、**対象期間中に計画再起動イベントは1件も発生していない**——`No events were found that match the specified selection criteria`。したがって仮にS-11が未設定のままだとしても、今週はそれを試す機会自体が無かった。5週連続で「未検証」の状態が続いている。

## 3. 改善案バックログ

**優先度: S=機能バグ級 / A=執行品質 / B=検証価値あり / C=様子見**

### S-12（新規）: watchdog再起動サイクルとMT5 LiveUpdateの衝突を検知し、バックオフまたは旧プロセス強制終了で復旧時間を短縮

- **仮説:** `check_and_recover.ps1`（`ops/watchdog_lib.ps1`）が5分間隔で再起動を試行する際、直前のLiveUpdateプロセスまたは終了未完了の旧`terminal64.exe`がコピー先ファイルをロックしたままだと、次の起動試行も同じ理由で失敗する。ジャーナルの`LiveUpdate failed to create copy ... [32]`シグネチャを検知したら、単純リトライではなく（a）該当端末の`terminal64.exe`系プロセスを完全終了させてから再起動する、または（b）次の再起動試行までの間隔を延長する（LiveUpdateの完了を待つ）ことで、無駄な3サイクル分（約30分）の遅延を圧縮できる可能性がある。
- **根拠:** F-2。8/26 06:35-07:12の約37分間、4回中3回の再起動試行がこの衝突で失敗し、4回目でLiveUpdateが介入しなかった回にのみ復旧に成功した。過去の類似停止（08-02・08-12・08-15）にはこのシグネチャが存在せず、既存のS-8（更新後EA未アタッチ検知）ではカバーされていない新しい故障モード。
- **検証方法:** バックテストではなく運用スクリプトの機能検証。`ops/test_watchdog_lib.ps1`と同様の単体テストで、ジャーナルに`LiveUpdate failed to create copy`パターンが出現するケースを模擬し、新しい検知・バックオフ（または強制終了）ロジックが正しく分岐することを確認。実運用では次回同型の事象発生時に復旧時間が約37分から短縮されるかを実測。
- **期待効果:** 同型障害の復旧時間短縮（現状30分刻みで最大4サイクル＝最大約35分の無駄を、検知後1〜2サイクルに圧縮）。実害は今回もゼロだったため緊急度は中程度。

### B-6（新規）: XM執行コスト劣後（B-3台帳）の要因深掘り

- **仮説:** B-3台帳で確認されたXM-OANDA間の一貫したマイナス差（全区間でXM劣後、POST_S4区間でむしろ拡大）は、単発の外れ値ではなく構造的な要因（XM側のスプレッド・スリッページ・約定執行速度のいずれか）による可能性がある。要因を切り分けられれば、バックテスト側の想定執行コストモデル（`docs/forward_test_baseline.md`）にXM分だけ追加のコスト補正を入れるべきか判断できる。
- **根拠:** F-3。N=22（判定ラインN≥20を突破）、累計-1,011円・平均-46.0円/ペア、PRE_S4(-28.7円/ペア)→POST_S4(-52.4円/ペア)で是正後にむしろ劣化。LOT_MISMATCHはゼロのためロット差由来ではなく、UNMATCHED6件は全てGBPJPYに集中しておりB-4（レンジ境界感度）とも関連しうる。
- **検証方法:** 台帳の個別ペアデータ（`cross_broker_ledger_out.csv`）を用い、通貨ペア別・時間帯別（特にGOLDの20:00強制決済 vs SCA JPY系の22:00決済）に劣後幅を分解。次にバックテスト側で同水準のXM側追加コスト（固定pips換算）を組み込んだ場合のSHARPE/PF変化をIS/OOS両期間で確認し、`docs/sca_scalping_backlog2.md`のゲート基準で採否判断。
- **期待効果:** 実際の執行コスト差をバックテストのコストモデルに反映できれば、フォワード期待値とバックテスト期待値の乖離（合格基準③）をより正確に説明できる。

### B-4（継続・変更なし）: SCA GBPJPYのレンジ境界感度分析（±2/5/10pips）

前回（08-22）から状況変化なし。08-21のF-2（片側単独エントリー・実損-1,260円）が優先度アップの根拠として既に確立済み。今週は新規のUNMATCHED追加（07-24 10:30・B-3台帳経由で判明）はあるが実損を伴う新事例ではない。着手判断はユーザー待ち。

### S-11（継続・5週連続未検証）: Windows「更新後の自動サインイン」設定

前回から仮説・検証方法・期待効果は変更なし。今回はF-6のとおりレジストリの直接確認自体がサンドボック制限でできなかったため、「未実施」の直接確認は4週目（08-22時点）が最後。ただし検証機会となる1074イベントも今週は発生しておらず、**設定状態にかかわらず5週連続で実地検証ができていない**点は変わらない。次回のルーチン実行では、レジストリ確認が可能な環境であれば再確認を行う。

### C-3（継続・改善傾向）: A-2 health_log検知の可視化不足

F-4のとおり今週は`health_log.md`の欠落自体がゼロだったため、C-3が想定する「検知はしているが伝わらない」状況は今週は発生していない。実装優先度は据え置き（低）とし、来週も欠落が発生しないか観察を継続する。2週連続で欠落ゼロが確認できれば、このバックログ項目はクローズ候補とする。

### C-4（新規・様子見）: result_log.md記帳スクリプトのXM有効証拠金計算に一過性の欠落

- **仮説:** F-5で観測された8/28の2回のみクレジットが計算式から漏れた事象は、記帳スクリプトの一時的な取得タイミング依存（API応答待ちのタイムアウトやリトライ漏れ）の可能性がある。
- **根拠:** F-5。実害なし・2回のみで再現性不明。
- **検証方法:** 次回以降も同型の欠落が発生するか観察を継続し、2回以上再現した場合のみ記帳スクリプトのログ・リトライロジックを確認する。
- **期待効果:** 記帳データの信頼性向上（EA稼働自体には無関係な補助記帳のため優先度は低い）。

**戦略提案なし** — 全枠が前回比で観察継続レベルの増分（SCA GOLDが11→15件で最も増加したが依然N<20、母集団分断の懸念があるSCA GBPJPYも判断保留を継続）であり、新たな戦略レベルの判断材料はない。

## 4. サンプルサイズの現状

累計決済数（全口座合計、2026-07-06以降。`2026-08-29_weekly.md` §3の確定値＝mixlog集計を採用）:

| 枠 | 累計決済数 | 累計損益(円) | 統計判断の目安 |
|---|---|---|---|
| SCA GBPJPY | 39（18+21） | +1,948（+2,175-227） | N≥30到達だが08-10のEA差替えで母集団分断のため一貫標本としては未達・判断保留継続 |
| SCA USDJPY | 14（7+7） | -2,727（-1,289-1,438） | 観察継続（<20） |
| SCA GOLD | 15（7+8） | -27,877（-13,201-14,676） | 観察継続（<20だが今週+4件で最も増分が大きい）。B-3(執行コスト側)は既にN=22到達済み（F-3・B-6参照） |
| RSI EURUSD | 10（うち2件は停止期間中のブローカー側SLでmixlog未記録） | 変化なし | 観察継続（<20） |
| RSI USDJPY | 7 | -3,913 | 観察継続（<10）。7戦全敗だが単一レジーム・少サンプルにつき戦略判断は保留継続 |
| VBO USDJPY | 6 | -1,479 | 観察継続（<10）・今週新規取引なし |
| PairTrade | 4 | +3,731 | 観察継続（<10） |
| RSI GBPUSD | 1 | +1,787 | 観察継続（<10） |
| PB USDJPY | 1 | -1,750 | 観察継続（<10） |
| PB GOLD | 1 | -11,365 | 観察継続（<10） |
| PB GBPJPY / Carry / ETH（決済ベース） / BTC_FUND / BfxRev | 0（ETHは保有中1本・含み+3,123が別枠であり未決済） | — | 未発火・判定不能継続（Carryは10週連続0取引・ヒステリシス採用後は年1-3回が正常のため異常ではない） |

いずれの枠も戦略レベルでの改善提案を出すには時期尚早（この原則は今週も維持）。

## 5. 次週の観察ポイント

1. **S-12（LiveUpdate衝突対策）の要否判断** — 実害はまだゼロだが、同型事象が再発した場合の復旧時間短縮効果を評価
2. **B-6の着手判断** — B-3台帳がN≥20を超えた初回として、XM執行コスト劣後の要因分析にユーザーがバックテスト検証で進むか
3. **health_log.mdの欠落ゼロが来週も続くか**（F-4・C-3）。2週連続ゼロならC-3をクローズ候補とする
4. **S-11の実地検証機会（1074イベント）の有無** — 5週連続未検証。次回発生時こそ真の検証機会
5. **SCA GOLDの累計決済数がN≥20に到達するか**（現在15件）。到達次第、戦略レベルの判定検討が可能になる
6. **result_log.mdのXM有効証拠金欠落（F-5）の再発有無**

---

*本レポートはClaude（自動巡回）が読み取り専用で作成。EA・設定・端末への操作、発注・決済は一切行っていません。改善案は提案のみであり、採否はバックテスト検証（every_tick・IS/OOSゲート）を経てユーザーが判断します。*
