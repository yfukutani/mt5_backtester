# フォワードテスト改善案 2026-08-22

対象期間: 前回改善案レポート（2026-08-15）以降＝2026-08-15〜08-22（実質5営業日 8/17-8/21）。
入力: [2026-08-22_weekly.md](2026-08-22_weekly.md)、mixlog CSV（3口座・7-8月分）、`launcher_log.txt`、`heartbeat_log.md`、`health_log.md`、Windowsイベントログ（読み取り専用）、`ops/cross_broker_ledger.py` v2実行結果。

## 1. 要約

**今週は稼働面のインシデントがゼロだった一方、それは対策の効果ではなく「試練が来なかっただけ」である点に注意が必要。** S-11（Windows「更新後の自動サインイン」設定）は今回もレジストリを直接確認したところ `AutoAdminLogon=0` のままで、2026-08-02の提案以来4週連続未実施。今週watchdogが48回/日×5営業日を完走できたのは、対象期間中にWindows Update計画再起動（イベントID1074）が一件も発生しなかったためであり、S-11の有効性はまだ一度も実地検証されていない。次にOS再起動が入れば07-31／08-14と同型の障害が再発するリスクは変わらず残っている。もう一つの新事実は、これまで「レンジ境界のわずかな乖離」として観察されてきたSCA GBPJPYのブローカー間差が、8/21に**初めて実損を伴う片側エントリー**という形で現実化したこと——XMのレンジ下限がOANDAより高かったため、同じ下落幅でXM側だけがブレイクと判定され、リバーサルBoost3倍(0.03lot)で単独エントリー・SL決済-1,260円となった一方、OANDAは境界内に収まり終日ノーエントリーだった。B-3台帳もこの1件をUNMATCHEDとして正しく検出しており、既存のB-4（レンジ境界感度分析）の優先度を上げる具体的根拠が揃った。B-3台帳自体はN=19/20まで到達し、来週にもA-4判定ラインに届く見込み。戦略レベルでは新規の判断材料なし（全枠が前回比±3取引以内の低増分）。

## 2. 観測された事実

### F-1（継続・最優先・S級）: S-11未実施が4週目、ただし今週は再起動トリガー自体が発生せず「未検証」のまま

`HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Winlogon` の `AutoAdminLogon` を今回も直接確認した結果 `0`（未設定）。
Windowsイベントログ（Id 1074, 直近15件）を確認したところ、直近の計画再起動は **08-14 03:30**（43時間停止の原因）が最後で、
対象期間8/17-8/21には1074イベントが**一件も発生していない**。これに対応して `launcher_log.txt` にも今期間
`PROBLEM DETECTED` は**ゼロ件**（前回同様の全文検索で確認）、`heartbeat_log.md` も08-15 22:20の復旧記録以降**新規アラート無し**。

**解釈の注意:** これは「S-11が効いた」証拠ではなく、単に検証機会（Windows Update再起動）が今週来なかっただけである。
過去の1074イベント間隔は07-07/07-08、07-17/07-19、07-31、08-14と不定期（10日〜2週間強）であり、次回の再起動発生時に
S-11が未設定のままだと**同型の40時間超停止が3度目発生する**リスクは全く軽減されていない。

### F-2（新規・A級・実損を伴う初事例）: SCA GBPJPYのレンジ境界差が「片側ブローカーのみエントリー＋リバーサルBoost」で実害化

8/21 09:00のレンジ確定値（`SCA_RANGE`実測）:

| ブローカー | 上限 | 下限 | 幅 |
|---|---|---|---|
| XM | 217.040 | **216.622** | 0.418 |
| OANDA FX | 217.052 | **216.465** | 0.587 |

下限差は約15.7pips（週次レポート⑤-2で既報）。この日、価格は216.62付近まで下落し、**XM側の下限(216.622)は下抜けたが
OANDA側の下限(216.465)には届かなかった**ため、XMのみが11:15にショートエントリー（`drift`がブレイク方向と逆＝リバーサル判定で
**Boost 3倍・0.03lot**）、OANDAは終日ノーエントリーとなった。XM単独ポジションは20:50にSLで決済され**-1,260円**。

これまでのF-4系の観測（08-05のBoost倍率差、08-11のロット差など）は「両ブローカーとも建玉したがロットが違う」型だったが、
**今回は「一方が建玉すらしない」型かつ建玉した側が3倍Boostを踏んだ**ため、実効リスクの集中度が過去最大。
B-3台帳（下記F-3）もこの1件を`[UNMATCHED] XM SCA_GBPJPY GBPJPY entry=2026-08-21 11:15`として正しく検出している。

### F-3（継続）: クロスブローカー執行コスト台帳v2（B-3）— N=19/20、累計 -946円

`ops/cross_broker_ledger.py`を2026年7-8月分mixlog全6ファイルで実行（`--s4-start "2026-07-20 18:07" --s4-end "2026-07-25 17:12"`、`docs/ops_fix_20260725.md`§5の確定値）。

```
matched pairs=27  in cost stats N=19  coverage=90%（excluded: TIMEDIFF=7, S4=2）
cumulative XM-OANDA diff = -946 JPY (mean -49.8 JPY/pair, N=19)
  PRE_S4    N=6  diff=-172 JPY
  S4_PERIOD N=1  diff=-53 JPY
  POST_S4   N=12 diff=-721 JPY
```

前回（08-15時点、N=18、累計-913円）からの増分は新規1ペアのみ（8/20 10:45 SCA_GBPJPY、XM-OANDA差-33円）。
LOT_MISMATCHは今週ゼロ。UNMATCHEDは既知4件＋F-2の8/21分1件が新規追加（計5件、いずれもGBPJPY）。
N=19はA-4判定ラインN=20まであと1件——来週のSCA系取引で到達する可能性が高い。

### F-4（継続・軽微だが悪化）: health_log.mdの平日更新欠落が2日→3日に拡大。ただしA-2自体は正しく検知している

`health_log.md`の最終更新は8/18で、次の記録は8/22（今回のルーチン実行時）まで飛んでおり、**8/19・8/20・8/21の3営業日が空白**
（前回08-15時点の2日欠落から悪化）。一方、`launcher_log.txt`を確認したところ、A-2（成果物鮮度監視）はこの欠落を
**リアルタイムで正しく検知していた**——8/19〜8/21の各日20:00以降、`A-2 health_log: no entry for today (2026-08-1X) by 20:00`
という行が30分ごとに記録され続けている。つまり**検知は機能しているが、その記録がlauncher_log.txt内に留まり外部通知に
つながっていない**ため、今回のような能動的な監査をしない限り気づかれない構造になっている。前週のF-5（heartbeatの
ログオン依存による検知不能）とは異なり、こちらは「検知はできているが可視化されない」という別種の問題である。
なお`health_log.md`自体はEA稼働そのものの安全性には影響しない補助的な記帳（実際のEA稼働健全性はwatchdog/launcher_log側で
別途確認済み・今週PROBLEM DETECTEDゼロ）であるため、緊急度は低い。

### F-5（新規・情報）: ETH枠がテスト開始来初めて発火（サンプル対象に追加）

8/20 00:01:37にXMでETHUSD 0.10lotのロングが成立（Carry流用ロジック、`Mult_ETH=2.0`×基本0.05lot=設計通り）。
現在も保有中（週次レポート時点で含み+4,490円）。決済後にサンプルサイズ表へ計上する。

## 3. 改善案バックログ

**優先度: S=機能バグ級 / A=執行品質 / B=検証価値あり / C=様子見**

### S-11（継続・4週連続未実施）: Windows「更新後の自動サインイン」設定

- **仮説:** 変更なし（2026-08-02提案・2026-08-15改善案で再確認済み）。ONにすれば次回Windows Update再起動でも
  watchdogの通常30分サイクル以内に復旧する。
- **根拠:** F-1。`AutoAdminLogon`レジストリ値を今回も実測し`0`のまま4週連続未実施を確認。過去2回（07-31・08-14）とも
  再起動発生時に42〜54時間の全断が実際に起きている。今週は再起動自体が発生しなかったため実地検証はできていないが、
  設定状態そのものは変わっていない。
- **検証方法:** 変更なし。ユーザーがGUIで設定後、次回のWindows Update再起動（意図的な検証再起動でも可）で
  「再起動後30分以内に3端末ともEA再アタッチ」を3/3で確認する。
- **期待効果:** 変更なし（未検知停止時間を40時間超級から30分級へ短縮）。

### B-4（優先度アップ・既存案の再評価）: SCA GBPJPYのレンジ境界感度分析（±2/5/10pips）

- **仮説:** SCA GBPJPYのレンジ算出（`SCA_RANGE`のfeed依存の高値・安値取得）に、ブローカー間フィード差を吸収する
  デッドバンド（境界を内側に2〜5pips縮小、または両ブローカーの実効レンジ交差判定を追加）を入れれば、
  F-2型の「片側だけブレイク判定」を減らせる。
- **根拠:** F-2（新規・実損-1,260円）。従来のB-4提案は「UNMATCHED5件」という頻度ベースの根拠だったが、
  今回**初めてBoost3倍が絡む片側単独エントリーが実損として顕在化**し、優先度を上げる具体的な金額的根拠ができた。
  B-3台帳のUNMATCHED件数もGBPJPYに集中（5/5件）しており、他ペア（USDJPY/GOLD）では発生していない。
- **検証方法:** バックテスト側で`SCA_RANGE`のOANDA/XM双方のヒストリカルフィードを用い、境界を±2/5/10pipsで
  縮小・拡大した場合のエントリー回数・IS/OOS両期間の損益変化を比較。デッドバンド導入がSHARPE/PFを悪化させないことを
  ゲート条件とする（`docs/sca_scalping_backlog2.md`準拠）。
- **期待効果:** ブローカー間のエントリー有無不一致を減らし、特にBoost発動時の実効リスク集中（今回のような片側3倍単独）を抑制。

### B-3（継続）: クロスブローカー執行コスト台帳 — N=19/20

前回から変更なし。次回N=20到達次第、A-4（ロット不一致の検知・除外・正規化）を適用し、
XM劣後傾向の正式判定に進む（現時点でLOT_MISMATCH該当は今週もゼロ）。

### C-3（新規・様子見）: A-2 health_log検知の可視化不足

- **仮説:** A-2はhealth_log.mdの平日欠落を正しく検知しているが（`launcher_log.txt`に記録）、
  能動的に見に行かない限り気づかれない。これを外部通知（既存のB-7で検討中のログオン非依存通知経路）に
  相乗りさせれば、今回のような3日間の見落としを防げる。
- **根拠:** F-4。8/19-21の3日間、A-2は20:00以降30分ごとに欠落を記録し続けていたが、通知先が無いため
  今回の週次改善案ルーチンまで誰も気づかなかった。ただしhealth_log.md自体はEA稼働の安全性とは無関係な
  補助記帳であり、実害はない。
- **検証方法:** B-7（heartbeatの外部通知経路）の実装可否検討時に、A-2のhealth_log/weekly artifacts検知も
  同じ通知経路に統合できるか合わせて設計する。単独では実装しない（優先度低）。
- **期待効果:** 監視系の検知漏れではなく「検知はしているが伝わらない」型の問題を横断的に解消。

**戦略提案なし** — 全枠が前回比で±3取引以内の低増分（大半のSCA枠以外は今週新規取引ゼロ）であり、
新たな戦略レベルの判断材料は無い。

## 4. サンプルサイズの現状

累計決済数（全口座合計、2026-07-06以降。`2026-08-22_weekly.md` §3の確定値＝mixlog集計を採用）:

| 枠 | 累計決済数 | 累計損益(円) | 統計判断の目安 |
|---|---|---|---|
| SCA GBPJPY | 33（15+18） | +2,994（+2,538+456） | N≥30到達だが08-10のEA差替えで母集団分断のため一貫標本としては未達・判断保留継続 |
| SCA USDJPY | 12（6+6） | -2,875（-1,371-1,504） | 観察継続（<20） |
| SCA GOLD | 11（5+6） | -14,325（-6,452-7,873） | 観察継続（<20）。B-3のXM劣後傾向はN=19/20で来週にも判定可 |
| RSI EURUSD | 10（うち2件は停止期間中のブローカー側SLでmixlog未記録・継続） | -3,308 | 観察継続（<20） |
| RSI USDJPY | 7 | -3,913 | 観察継続（<10）。7戦全敗だが単一レジーム・少サンプルにつき戦略判断は保留継続 |
| VBO USDJPY | 6 | -1,479 | 観察継続（<10） |
| PairTrade | 4 | +3,731 | 観察継続（<10） |
| RSI GBPUSD | 1 | +1,787 | 観察継続（<10） |
| PB USDJPY | 1 | -1,750 | 観察継続（<10） |
| PB GOLD | 1 | -11,365 | 観察継続（<10） |
| PB GBPJPY / Carry / ETH（決済ベース） / BTC_FUND / BfxRev | 0（ETHは保有中1本・含み+4,490が別枠であり未決済） | — | 未発火・判定不能継続（Carryは9週連続0取引・ヒステリシス採用後は年1-3回が正常のため異常ではない。ETHはテスト開始来初発火だが未決済） |

いずれの枠も戦略レベルでの改善提案を出すには時期尚早（この原則は今週も維持）。

## 5. 次週の観察ポイント

1. **S-11（「更新後の自動サインイン」設定）がONにされたか — 4週連続の最優先観察事項。** 次回Windows Update再起動
   （1074イベント）が発生した際に、実地で30分以内復旧を確認できるかが本当の検証になる
2. **B-4のレンジ境界感度分析の着手判断** — F-2の実損事例（-1,260円）を受け、ユーザーがバックテスト検証に進めるか
3. **B-3がN=20に到達するか**（現在19）。到達次第、SCA GOLDのXM劣後傾向をA-4のロット補正込みで正式判定
4. **health_log.mdの平日更新再開の有無**（F-4）。改善が見られなければC-3の実装優先度を再検討
5. **ETH建玉の決済結果**（初回のため決済時の挙動・ロジックが設計通りか確認）
6. **8/19-8/21の3日間 health_log欠落の原因**（記帳ルーチン自体の不調か、単なる記入漏れか）を可能なら特定

---

*本レポートはClaude（自動巡回）が読み取り専用で作成。EA・設定・端末への操作、発注・決済は一切行っていません。改善案は提案のみであり、採否はバックテスト検証（every_tick・IS/OOSゲート）を経てユーザーが判断します。*
