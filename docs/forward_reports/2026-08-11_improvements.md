# フォワードテスト EA改善案 2026-08-11

対象期間: **2026-08-03（月）〜 2026-08-11（火）**（7営業日・前回改善案が 08-02 のため9日ぶん）
入力: `2026-08-11_weekly.md`（20:19生成・存在を確認）／ mixlog 3口座（07月・08月）／ 端末ジャーナル ／
`launcher_log.txt` ／ `heartbeat_log.md` ／ タスクスケジューラ定義 ／ B-3台帳v2 ／ リポジトリのコミット履歴

**取引環境には一切変更を加えていない**（EA・.set・端末設定・発注/決済すべて未操作）。
本レポート作成時の口座情報取得には、前回導入した `ops/mt5_read.py`（1端末=1サブプロセス・読み取り専用）を使用した。

---

## 1. 要約

**今週の最大の収穫は、7月に「仮説」だったF-5（リバーサルBoostのフィード依存）が実損益を伴って再現し、
同時にB-3台帳の設計欠陥まで暴いたこと。** 08-05のSCA GBPJPYで drift が OANDA **−0.023** / XM **+0.060** と符号が割れ、
**同一シグナルで OANDA 0.02lot（Boost発動）/ XM 0.01lot** という実効リスク2倍の差が発生した。
この1件がB-3台帳に **−185円** として計上されており、**累計 −686円の27%・POST_S4区間 −461円の40%** を占める。
しかしこれは執行コスト（スプレッド・スリッページ）ではなく**ロット差**であり、
**B-3は「執行コスト」を測る目的なのに、ロット不一致を検知も分離もせず同じ列に混ぜている**（新規 F-3）。
一方 **F-4（GBPJPYレンジ乖離）は大幅に縮小**（|下限差| 5.2→2.4 pips、|レンジ幅差| 11.2%→4.5%）し、
台帳の UNMATCHED も**今期間の新規発生ゼロ**。7月の乖離はボラティリティ由来の一過性だった可能性が高まり、B-4の緊急度は下がった。
インフラ面では、**S-5（watchdogのログオン非依存化）が合格試験1回目で明確に失敗し撤回された**（セッション0はチャート生成不可＋IPCポート競合）。
これは正しい判断だが、**代替策（Windows「更新後の自動サインイン」）が未設定のため 07-31 型の穴は開いたまま**である。
さらに **S-7ハートビートはこのS4U→Interactive変更を検知できなかった** — 監視対象タスクの「状態」しか見ておらず「構成」を見ていないためで、これは私の実装の穴（新規 F-2）。
新規改善案は **5件（S級2・A級1・B級2）**、最高優先度 🔴 S。**戦略パラメータの変更提案は今週も無し。**

---

## 2. 観測された事実

### 🔴 F-1（機能・S級・継続）S-5は撤回され、07-31型の穴は**開いたまま**

**経緯（リポジトリと実機の突合で確定）:**

| 日時 | 出来事 |
|---|---|
| 08-02 19:0x | `fix_watchdog_admin.ps1` を管理者実行 → `LogonType=S4U` / `RunLevel=Highest` / BootTrigger追加。S4U下での定時実行も確認 |
| 08-02深夜〜08-03 | **S-5合格試験を実施 → 1回目で3端末とも失敗** |
| 08-03 00:45:14 | タスク定義を書き換え（`Interactive` / `Limited` へ切り戻し・管理者権限） |
| 08-03 00:48 | commit `9fa72b6`「S-5合格試験の結果(1回目で失敗)とInteractive切り戻しを記録」 |

**失敗の中身（想定より深刻だった）:**

| 端末 | 実際に起きたこと |
|---|---|
| OANDA_FX / XM | `MDI create failed` / `open chart 'USDJPY' failed for 'MIX_EA_OANDA'` — **セッション0にはデスクトップが無く、チャート自体を作れない**。EAが載る土台が存在しない |
| OANDA_CFD | `MCP bind error on 127.0.0.1:22346 (10048)` → 起動直後にクラッシュ。**全端末が単一の内部IPCポートを共有**している |

前回の改善案 §2.4 は「セッション0では見えないだけで動くかもしれない」と想定していたが、**実際は「そもそも載らない」**。
**S4Uは筋の悪い解であることが実測で確定した。撤回は正しい判断である。**

**現在の実機状態（2026-08-11 20:25 実測）:**

```
LogonType = Interactive   RunLevel = Limited
Trigger   = TimeTrigger(30分反復) + BootTrigger
Description = "EA MT5 watchdog. S4U + AtStartup so it survives an unattended reboot (S-5)."  ← 実装と不一致
ActiveHoursStart=9 / ActiveHoursEnd=3 / SmartActiveHoursState=0   ← S-6は適用済み
AutoAdminLogon = 0（「更新後の自動サインイン」未設定）
```

- **BootTrigger は `Interactive` では実質no-op**（対話セッションが無ければ発火しても実行されない）
- **したがって「PCが無人再起動されるとEAが戻らない」という 07-31 の構造は温存されている**
- S-6により**07-31型（11:30 JSTの自動再起動）は阻止できる**ので発生確率は下がったが、これは予防であって復旧ではない
- 08-03以降 **再起動が一度も発生していない**（uptime 8.87日）ため、この穴は今期間は顕在化していない

**なお、週次レポートはこれを「説明と実装の食い違い」＝是正漏れと評価しているが、正確には
「合格試験で失敗し意図的に撤回された結果」である。** ただし **`Description` が S4U のまま残っていることが
レビュアーを誤読させた**のは事実で、これ自体が直すべき欠陥（S-9）。

### 🔴 F-2（監視・S級・**自分の実装の穴**）S-7ハートビートは watchdog の「構成変更」を検知できない

- 08-03 00:45 にタスクの `LogonType` が **S4U → Interactive**、`RunLevel` が **Highest → Limited** に変わったが、
  **`heartbeat_log.md` には一行も記録されていない**（最終エントリは 08-02 23:51）
- 原因は `ops/heartbeat_lib.ps1` の `Test-WatchdogTaskEnabled` が見ているのが
  **`State`（Disabled か）と `LastTaskResult`（異常終了か）だけ**で、
  **`LogonType` / `RunLevel` / トリガー構成を一切検証していない**こと
- 今回の変更は**意図的**だったので実害は無い。しかし S-7 の存在意義は
  「**安全網そのものが黙って別物にすり替わっていないか**を外から見張ること」であり、
  **監視対象の最も重要な属性（＝ログオン非依存かどうか）を見ていないのでは目的を果たしていない**
- 07-31障害の根因はまさに `LogonType=InteractiveToken` だった。**その属性を監視項目に入れていなかったのは設計漏れ**

**S-7の他の面は良好:** 08-03〜08-11 の9日間で **検知0件・誤検知0件**。
watchdog は 8日間 48回/日を欠落なく完走し、ハートビートも 30分ごとに `LastTaskResult=0` で稼働継続（20:20:01 最終）。
267009誤報の修正（良性HRESULT許可リスト）も効いており再発なし。

### 🟠 F-3（台帳・新規）B-3が**ロット不一致を執行コストとして誤計上**している

**08-05 SCA GBPJPY の実測（mixlogから直接確認）:**

| | drift | ATR(D1) | Boost判定 | ロット | 建値 | 実現 |
|---|---:|---:|---|---:|---:|---:|
| OANDA FX | **−0.023** | 1.74793 | 逆符号 → **発動** | **0.02** | 212.247 | **+258** |
| XM | **+0.060** | 1.71229 | 同符号 → 不発動 | **0.01** | 212.282 | **+73** |

- 建値差はわずか3.5pips、決済も45秒差。**損益差 −185円のほぼ全部が「ロットが2倍か否か」で説明される**
- ところが台帳の出力は `lot=0.01`（XM側のみ記録）・`diff_xm_minus_oanda=-185`・`in_stats=1` で、
  **ロット不一致の警告は一切出ない**
- 影響度: **累計 −686円のうち −185円（27%）**、**POST_S4区間 −461円のうち −185円（40%）** がこの1件
- B-3の目的は「XMとOANDAのどちらが執行コストで有利か」を測ることなので、
  **ロット差由来の損益を混ぜると結論が歪む**。現在の平均 −42.9円/ペアは過大評価されている可能性が高い

**台帳の他の進捗は良好:** **N=9 → N=16/20**、カバー率 85% → **89%**、
**今期間の新規 UNMATCHED はゼロ**（既存5件はすべて7月分）、**新規 TIMEDIFF もゼロ**。

### 🟢 F-4（執行品質・**改善**）GBPJPYのレンジ乖離は大幅に縮小した

`SCA_RANGE` のブローカー間突合（7月18営業日 vs 8月7営業日）:

| 銘柄 | 期間 | N | \|下限差\| 平均 | \|レンジ幅差\| 平均 | \|レンジ幅差\| 最大 |
|---|---|---:|---:|---:|---:|
| **GBPJPY** | 7月 | 18 | **5.2 pips** | **11.2%** | **35.2%** |
| **GBPJPY** | **8月** | 7 | **2.4 pips** | **4.5%** | **19.2%** |
| USDJPY | 8月 | 7 | 1.3 pips | 0.9% | 2.2% |
| GOLD | 8月 | 7 | 12.7 pips(=$0.13) | 1.8% | 9.0% |

- **下限差・レンジ幅差ともおよそ半減**。台帳の UNMATCHED も今期間ゼロで、**シグナルの発火不一致は再発していない**
- ただし **GBPJPY だけ下限が上限より乖離しやすい非対称性は残る**（下限2.4 vs 上限1.3 pips）。
  08-06 は依然として **レンジ幅 −19.2%** の差
- **解釈:** 7月の大きな乖離は恒久的なフィード品質差ではなく、**相場のボラティリティ局面に依存する**可能性が高い。
  B-4（感度分析）の**緊急度は下げてよい**が、`|レンジ幅差|` が二桁%に戻る局面がある以上、廃案にはしない

### 🟠 F-5（設計と実挙動の乖離・**再現確定**）Boostのフィード依存は「仮説」から「実証済み」に変わった

- 07-28 GBPJPY（drift OANDA +0.023 / XM −0.005）に続き、**08-05 に2件目が発生**（OANDA −0.023 / XM +0.060）。
  **今回は実際にロットが 0.02 vs 0.01 に分かれ、損益差 −185円として顕在化した**（F-3）
- drift符号不一致の頻度: 7月 3/18営業日（17%）→ **8月 2/7営業日（29%）**（08-05・08-06）。
  8月の2件はいずれも **|drift| < 0.08** の微小値
- **提案中のデッドバンド k=0.05 を当てると、既知の2件とも解消される**:

  | 日付 | ブローカー | \|drift\| | k=0.05 × ATR | 判定 |
  |---|---|---:|---:|---|
  | 07-28 | OANDA | 0.023 | 0.0532 | 不発動 ✅ |
  | 07-28 | XM | 0.005 | 0.0523 | 不発動 ✅ |
  | 08-05 | OANDA | 0.023 | 0.0874 | 不発動 ✅ |
  | 08-05 | XM | 0.060 | 0.0856 | 不発動 ✅ |

  → **2/2 の乖離が両ブローカーとも「Boostなし」に収束**する
- **公平な反証も記しておく:** 08-05 の OANDA 側の Boost は **実際に利益を増やした**（+258 vs XM +73）。
  デッドバンドを入れていればこの利益は得られなかった。**N=1 では損益の優劣は判断できない**。
  B-5の論点は「損益」ではなく「**同一シグナルで実効リスクが2倍ぶれない**」ことである点を、判定時に取り違えないこと

### 🟡 F-6（執行品質・N=3）SCA GOLD の20:00強制決済のXM遅延は縮小

- 07-21 **+94秒** → 07-28 **+95秒** → **08-10 +46秒**。**3件とも XM が後**という方向は一貫するが、**大きさは半減**
- 08-10 は EA が v1.5 に差し替わった当日でもあり、**遅延縮小がEA変更由来か相場由来か切り分けできない**
- 損益影響は 08-10 で −26円（XM −266 / OANDA −240）と軽微。**引き続き観察のみ**

### 🟡 F-7（執行品質・新規）VBO の**エントリー**が XM で94秒遅延

- 08-06 20:01:34 の VBO USDJPY で `entry_diff_s = +94`（XMが後）。決済側は 0秒差
- これまでF-6で見ていたのは決済遅延だったが、**エントリー側にも同規模の遅延が出た**のは初
- ただし **VBO枠は 08-10 に本番ポートフォリオから除外された**（⚠️B）ため、**この枠での追試はできない**。記録のみ

### ⚪ F-8（インフラ・良好）S-6・S-8は機能し、稼働は完璧だった

- **S-6適用済み**（ActiveHours 9→3・`SmartActiveHoursState=0`）。**07-31型の11:30 JST再起動は現設定なら阻止される**
- **S-8統合完了**（08-02 22:38・テスト27/27）。`check_and_recover.ps1` に
  `S-8 AutoTrading check: OK - enabled on all reachable terminals` が毎回出力されており、
  **「EAは載っているが自動売買OFF」の無音故障は塞がれた**
- 期間中 **稼働停止ゼロ・`PROBLEM DETECTED` 0件・`ESCALATION` 0件**、SCA_RANGE/DAILY 全営業日出力、
  mixlog⇔MT5 3口座とも完全一致

### ⚪ F-9（分析上の注意）08-10 のEA差し替えで**フォワードの母集団が分断された**

- 08-10 07:33〜07:38 に `MIX_EA_OANDA v1.0→v1.2` / `MIX_EA v1.3→v1.5`、**VBO枠を停止**、
  SCA/PB/RSIのパラメータ変更、**Boost欠落修正**が同時投入された
- **F-5/F-6/B-3 の追跡はすべて 08-10 を境に条件が変わっている**。本レポートの数値は
  「08-10前=旧EA」「08-10以降=新EA」の混合であり、**今後は分割集計が必要**
- 特に **Boost欠落修正**が入ったため、**F-5のBoost発動頻度そのものが今後変わる可能性がある**

---

## 3. 改善案バックログ

> 実装は一切していない。運用インフラ案（S-9/S-10/A-4）はバックテスト対象外なので再現テストの合格条件を定義した。
> 戦略側（B-4/B-5）は `docs/sca_scalping_backlog2.md` の検証プロトコル（every_tick・スプレッド実費込み・IS/OOS両期間プラス）を通す。

### 🔴 S-9（新規・最優先）S-7ハートビートに「watchdogの構成監視」を追加する

- **仮説:** `Test-WatchdogTaskEnabled` が `LogonType` / `RunLevel` / トリガー種別を**期待値と突合**するようにすれば、
  安全網が黙って別物にすり替わる事故を検知できる
- **根拠（観測データ）:** F-2。08-03 00:45 の S4U→Interactive 変更を**S-7は一行も記録しなかった**。
  07-31障害の根因そのものが `LogonType` だったのに、その属性が監視対象に入っていない
- **検証方法（合格条件）:**
  1. 期待構成を設定ファイル（例 `forward_test/watchdog_expected.json`）に外出しし、差分があれば `heartbeat_log.md` に `ALERT` を出す
  2. 使い捨てタスクで `LogonType` を変える／トリガーを削る／`RunLevel` を下げる の3パターンを作り、**3/3 で検知**すること
  3. 正常構成では1週間 **誤検知0件**
  4. 既存の `ops/test_heartbeat_lib.ps1`（現在36/36）に上記の回帰テストを追加し全件PASS
- **期待効果:** 「安全網が黙っている＝正常」の最後の死角を塞ぐ。
  **今回は意図的な変更だったので無害だったが、次も意図的とは限らない**

### 🔴 S-10（新規）タスクの `Description` を実装と一致させ、両者の乖離を検知対象にする

- **仮説:** `Description` が実装と食い違っていること自体が**レビューを誤らせる欠陥**である。
  記述を正し、S-9の構成監視に含めれば再発しない
- **根拠（観測データ）:** F-1。現在の `Description` は
  `"S4U + AtStartup so it survives an unattended reboot (S-5)."` だが実装は `Interactive`。
  **今週の週次レポートはこれを読んで「説明と実装の食い違い＝是正漏れ」と評価した**（実際は意図的な撤回）。
  同種の誤読は 07-31 の初回調査でも起きており（当時の `Description` も "logon-independent" と書かれていた）、**2回目**である
- **検証方法（合格条件）:** `Description` を実態（`Interactive` + 30分間隔 + BootTrigger は保険、
  無人再起動時は自動サインイン設定が前提）に書き換え、S-9の突合対象に含める。
  レビュー時に `Description` と `LogonType` の組み合わせが矛盾していたら `ALERT`（1/1で検知）
- **期待効果:** 週次レビューの誤読コストをゼロにする。**同じ誤読を2回している**ので費用対効果は高い

### 🟠 A-4（新規）B-3台帳に**ロット不一致の検知と正規化**を入れる

- **仮説:** 台帳が `xm_lot` / `oanda_lot` を別々に持ち、不一致なら
  (a) `LOT_MISMATCH` を警告、(b) 既定でコスト集計から除外、(c) 1ロットあたりに正規化した差も併記する、
  とすれば「執行コスト」と「ロット差」が混ざらなくなる
- **根拠（観測データ）:** F-3。08-05の1件が**累計の27%・POST_S4区間の40%**を占めており、
  かつ**それは執行コストではない**。現行の平均 −42.9円/ペアは過大評価の疑いが強い
- **検証方法（合格条件）:**
  1. `ops/test_cross_broker_ledger.py`（現在15/15）に **ロット不一致ケースの回帰テストを追加**し全件PASS
  2. 08-05のペアが `LOT_MISMATCH` で**集計除外**され、**累計が −686円 → −501円** に補正されることを実データで確認
  3. 除外前後の N・平均・セグメント別内訳を両方レポートに出す（隠さない）
- **期待効果:** ブローカー選択（XM継続 / OANDA一本化）の判断根拠から交絡を除く。
  **N=20到達時に誤った結論を出すのを防ぐ**（現在N=16なので残り4ペア＝あと2〜3週で判断時期に入る）

### 🔵 B-5（継続・**優先度を上げる**）リバーサルBoost の drift デッドバンド

- **今週の更新:** F-5で**2件目が実損益を伴って再現**し、**提案中の k=0.05 が既知2件を2/2で解消する**ことを確認（§F-5の表）。
  7月時点では「フィード差で符号が割れうる」という**可能性**の指摘だったが、**現実に実効リスクが2倍ぶれた事例が2件**になった
- **検証方法:** 前回と同一（`configs/b5_sca_gbpjpy_boost_deadband_is.yaml` / `_oos.yaml` で
  k = 0/0.05/0.10/0.15/0.20 をスイープ、every_tick、IS/OOS両期間プラス維持＋月利劣化10%未満）。
  **`UseReversalBoost:false` のベースラインを必ず別途1本走らせ、Boost自体の寄与を分離すること**
- **⚠️ 判定時の注意（新規）:** 08-05の Boost は**実際に利益を増やした**（+258 vs +73）。
  デッドバンドは**その利益を捨てる**方向に働く。**B-5の判定基準は損益ではなく「実効リスクの一貫性」**であり、
  月利が多少劣化しても許容するのか否かは、ゲート通過後に**ユーザーが方針として決める**べき論点
- **⚠️ 前提の変化:** 08-10 に**Boost欠落修正**が入った（F-9）。**まず修正後のEAで k=0 の挙動を確認**してから
  スイープすること。修正前の結果とは比較できない
- **期待効果:** 同一シグナルでブローカー間の実効ロットが2倍ずれる事象の解消

### 🔵 B-4（継続・**優先度を下げる**）SCA GBPJPY のレンジ境界感度

- **今週の更新:** F-4のとおり **乖離が約半減**（|下限差| 5.2→2.4 pips、|レンジ幅差| 11.2%→4.5%）し、
  **UNMATCHEDの新規発生ゼロ**。「実測フィード差5.2pipsでエッジが壊れるか」という当初の問題設定は、
  **その5.2pipsという前提自体が7月固有だった可能性**が出てきた
- **したがって:** 検証プロトコル（`b4_*.yaml`・±10pips step1 のスイープ）は**そのまま維持**するが、
  **B-5より後回しでよい**。摂動幅の判定基準は 5.2pips ではなく
  **「観測された最大値」＝ 19.4 pips（07-30）と「直近平均」＝ 2.4 pips の両方**で見ること
- **期待効果:** 変更なし（枠の採否・ブローカー一本化の判断根拠）

### ⏸ S-5（撤回済み・**代替策が未完了**）

- S4Uは**実測で不可**と確定したため**再提案しない**（F-1）
- **ただし残作業がある**: Windowsの
  **設定 → アカウント → サインイン オプション → 「更新後にサインイン情報を使ってデバイスのセットアップを自動的に完了する」を ON**。
  これが未設定のため、**無人再起動が起きれば 07-31 と同じ結果になる**
- **資格情報が絡むためアシスタントは代行しない。ユーザー操作が必要**
- S-6により発生確率は下がっているが、**確率を下げただけで穴は開いている**という認識を維持すること

### ⏸ 継続監視（新規提案なし）

- **A-1（GOLD枠のリスク過大）:** ユーザー決定済み（フォワード中は現状維持）につき**再提案しない**。
  今週の実測のみ記録: XAUUSD 0.01lot の必要証拠金が **34,933円（テスト開始以来の最高値）**＝CFD口座 equity の **35.0%**、
  維持率287.4%。累計損失 −25,381円のうち **GOLD系2枠で −19,991円（79%）**
- **IPCポート競合（127.0.0.1:22346）:** S-5試験で判明した「全端末が単一の内部IPCポートを共有」する挙動。
  watchdogが短時間に複数端末を連続復旧する現行設計と干渉しうるが、
  **Interactive環境では 08-02 の3端末連続復旧が16〜20秒間隔で全て成功しており、今期間も復旧イベント自体がゼロ**。
  **観察継続とし、復旧失敗が観測されたら起票する**

---

## 4. サンプルサイズの現状

決済（OUT）件数の累計・**口座横断**（週次レポート③より）。

| 枠 | 累計決済 | 累計実現 | 判定 |
|---|---:|---:|---|
| SCA GBPJPY | **30** | +2,417 | 唯一30到達。ただし後述の理由で判断保留 |
| SCA USDJPY | 12 | −2,875 | 保留 |
| RSI EURUSD | 8 | −3,308 | 保留 |
| SCA GOLD | 7 | −8,626 | 保留 |
| RSI USDJPY | **7**（初稼働・全敗） | −3,913 | 保留 |
| VBO USDJPY | 6 | −1,479 | **08-10に枠停止**（以後増えない） |
| PairTrade | 4 | +3,731 | 保留 |
| PB GOLD / PB USDJPY / RSI GBPUSD | 各1 | −11,365 / −1,750 / +1,787 | 保留 |
| Carry / PB GBPJPY / ETH / BTC_FUND / BfxRev | **0** | — | 未発火（設計どおりの頻度） |

**統計的判断が可能になる目安 — 今週から前提が変わった:**

- **SCA GBPJPY が30決済に到達した**が、**これは口座横断の合計**でありブローカー別では各15前後。
  加えて **08-10のEA差し替え（F-9）で母集団が分断**されたため、
  **「30取引ぶんの一貫した標本」は存在しない**。**引き続き判断保留とする**
- **今週から累計の意味が変わった点を明記しておく:** 08-10以降は v1.2/v1.5・VBO停止・SCA/PB/RSIパラメータ変更後の別物である。
  **今後の枠別評価は 07-06〜08-07 と 08-10以降 で分割**しなければ意味を持たない。
  分割すると**どの枠も新レジームでは1桁の標本**に戻る
- **RSI USDJPY の7連敗（−3,913円）について:** 初稼働で全敗だが、
  USDJPY が 156.1→159.3（+2.0%）と一方向に上昇する局面での逆張りショート反復であり、**設計どおりの挙動**。
  **7取引・単一レジームでの全敗をもって戦略の欠陥と判断しない**。観察継続

---

## 5. 次週の観察ポイント

1. **「更新後の自動サインイン」がONにされたか** — されていなければ 07-31 型の穴は開いたまま。**最優先**
2. **無人再起動が発生した場合の実挙動** — 08-03以降 再起動ゼロのため、S-6適用後の初回再起動が最初の実地試験になる
3. **F-5の3件目** — drift符号不一致とロット差が再発するか。**08-10のBoost欠落修正後の初サンプル**になる点に注意
4. **B-3が N=20 に到達するか**（現在16）。到達したら **A-4のロット不一致補正を入れてから**採否判断すること
5. **F-4の推移** — GBPJPYのレンジ乖離が2.4pips水準で安定するか、7月水準に戻るか
6. **08-10以降の新EA（v1.2/v1.5）での枠別成績** — 旧レジームと分けて集計する
7. **GOLD建玉の帰趨** — 08-11に建てた両口座のSCA GOLDショート（含み −10,225円）が20:00強制決済でどう着地したか。
   XAUUSD証拠金は過去最高の34,933円（CFD equity の35%）
8. **Carry AUDJPY が7週連続0取引** — 合格基準①のチェック項目が判定不能のまま。ヒステリシス採用後は年1-3回が正常なので異常ではない
