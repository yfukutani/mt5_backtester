# Codex の第7ラウンド案（原文・2026-09-19）

実行: `codex.exe exec -m gpt-5.6-terra --sandbox read-only`（`backtest.ini` は読ませていない。
出力の冒頭でその旨を明記させている）。依頼文は「第1〜4ラウンドの doc と棄却記録を読んだうえで、
**まだ測っていない軸**を、とくに退出・執行／Pair／Carry／RSI の退出側で 20〜30案」。

仕分けは [oanda_fx_sca_reversal_subset_20260919.md](oanda_fx_sca_reversal_subset_20260919.md) §4。

> ⚠️ **依頼文の不備:** Codex には `MQL5/Experts/MIX_EA_OANDA.mq5` を読ませたが、
> 測定に使う EA は `experts/MIX_EA_SIMVERIFY.mq5`（ラボ版）である。
> そのため Codex の実装対効果 第1位 `P019`（`Carry_ExecTF` の H1/D1 比較）は
> **SIMVERIFY に存在しない入力**で、測れない。次回は EA のパスを明示すること。

---
`backtest.ini` は読んでいません。EA本体と指定の全資料を確認しました。

重要な実装確認です。RSI 3枠は「反対シグナルまで保有」だけではありません。各注文に固定SL/TPが入り、SL/TP未到達の場合にのみ、反対側の有効シグナルで既存建玉を閉じてドテンします。現行値は UJ/GU が SL 50 pips / TP 110 pips、EU が SL 25 / TP 105 です。`UseProfitTrail` は既存inputですが、Pair・Carry・GOLDを除くFX全枠に一括適用され、枠別にはできません。

以下、既測定軸を避けた24案です。効き幅は「ブック全体・固定サイジングのOOS単利月利」の増減pt見込みです。

### P001 枠: RSI USDJPY / RSI EURUSD / RSI GBPUSD 軸: 退出
- **変更内容**: 既存input `UseProfitTrail`, `ProfitTrail_Step`, `ProfitTrail_Lock` で実行可能。ただし3 RSI枠だけに分離するにはコード変更が要る。
- **期待する機構**: TP前に伸びた反転ポジションが戻る損失を、段階的SLで回収する。
- **効き幅の見積もり**: -0.03〜+0.03pt。
- **反証条件**: 両窓で純益低下、またはequity DD低下だけで純益を犠牲にするなら棄却。

### P002 枠: RSI USDJPY 軸: 退出
- **変更内容**: コード変更。含み益が+1RでSLを建値、+1.5Rで+0.5Rへ移す枠別BE。
- **期待する機構**: H4反転の「一度は正しかったがTP前に戻る」トレードを負けから除外する。
- **効き幅の見積もり**: -0.01〜+0.02pt。
- **反証条件**: TP到達利益の減少がBEで防いだ損失を上回る、または両窓の一方で悪化。

### P003 枠: RSI EURUSD 軸: 退出
- **変更内容**: コード変更。+1R建値移動、または+2Rのみ建値移動を比較。
- **期待する機構**: H1の固定TP 105 pipsはSLの4.2倍で遠い。短期的な逆行を利益化して分布を改善できる可能性がある。
- **効き幅の見積もり**: -0.03〜+0.02pt。
- **反証条件**: EURUSD固有の窓間不安定性が退出側にも現れ、符号が割れるなら棄却。

### P004 枠: RSI GBPUSD 軸: 退出
- **変更内容**: コード変更。+1Rで半分決済、残りは現行TP/SLのまま。
- **期待する機構**: GBPUSDのBB単独シグナルは両窓で正だったため、勝ちを一部確定して左尾を縮められるかを独立に問える。
- **効き幅の見積もり**: -0.02〜+0.02pt。
- **反証条件**: 半分を早く落とすことによるTP利益の減少が、損失圧縮を上回れば棄却。

### P005 枠: RSI 3枠 軸: 退出
- **変更内容**: コード変更。TPを固定pipsから「エントリー時MAまで」または「BB中央線まで」に置換する。
- **期待する機構**: 平均回帰戦略なら、固定距離より回帰目標に利確を合わせるほうが機構整合的。
- **効き幅の見積もり**: -0.05〜+0.04pt。
- **反証条件**: 既存TPより近くなって利益を取り切れず、両窓で純益が下がるなら棄却。

### P006 枠: RSI 3枠 軸: 退出
- **変更内容**: コード変更。保有時間上限（EUは24/48 H1バー、UJ/GUは12/24 H4バー）を追加。
- **期待する機構**: 回帰が一定時間内に起きないなら、元の逆張り仮説が弱い状態とみなして資本拘束を切る。
- **効き幅の見積もり**: -0.04〜+0.03pt。
- **反証条件**: 長期保有側に主要利益が集中し、時間決済で両窓とも悪化すれば棄却。

### P007 枠: RSI 3枠 軸: 退出
- **変更内容**: コード変更。反対シグナルでの現行ドテンを「決済後、次バーまで再エントリー禁止」にする。
- **期待する機構**: 完全なドテン停止は既に棄却済みだが、同一バーでの反転建玉だけを避けるのは別軸。
- **効き幅の見積もり**: -0.02〜+0.02pt。
- **反証条件**: 反転直後の継続トレンド利益を失い、両窓で悪化なら棄却。

### P008 枠: Pair EURUSD/GBPUSD 軸: 入口
- **変更内容**: コード変更。`entryZ=4.0`より高い4.25 / 4.5を試すinput化。
- **期待する機構**: 3.0・3.5へ緩める方向とは逆に、より極端な乖離だけを選び、往復コストを上回る収束余地を要求する。
- **効き幅の見積もり**: -0.02〜+0.03pt。
- **反証条件**: 取引数だけが減り、1脚当たり損益も改善しないなら棄却。

### P009 枠: Pair EURUSD/GBPUSD 軸: 退出
- **変更内容**: コード変更。`exitZ=-1.0`を -0.5 / 0.0 / +0.5 に入力化して比較。
- **期待する機構**: 現行はゼロ回帰を越えて反対側まで待つため、平均回帰の利益を戻している可能性がある。
- **効き幅の見積もり**: -0.03〜+0.04pt。
- **反証条件**: 早過ぎる決済で大半の期待値を捨て、両窓で低下なら棄却。

### P010 枠: Pair EURUSD/GBPUSD 軸: 退出
- **変更内容**: コード変更。Zが一度 `exitZ` に届いたら、その後の逆行で即時決済する「回帰後トレーリング」。
- **期待する機構**: 収束を確認した後の再乖離を、現行の固定閾値より早く切れる。
- **効き幅の見積もり**: -0.02〜+0.03pt。
- **反証条件**: Zの通常揺れで利確を細切れにし、平均利益が低下すれば棄却。

### P011 枠: Pair EURUSD/GBPUSD 軸: 退出
- **変更内容**: コード変更。保有上限を既測定の240/480本以外、短い72/120本または長い720本で測る。
- **期待する機構**: 既測定点が粗いため、短期の収束失敗を切る領域と長期収束を待つ領域を分けて確認する。
- **効き幅の見積もり**: -0.03〜+0.03pt。
- **反証条件**: 全点で現行の無制限保有以下、または窓間で明確に符号反転なら棄却。

### P012 枠: Pair EURUSD/GBPUSD 軸: 入口・執行
- **変更内容**: コード変更。価格差ではなく、過去200本のOLSヘッジ比率で残差Zを算出し、建玉量もその比率に合わせる。
- **期待する機構**: 現行は `EURUSD - GBPUSD` と各0.01ロットで、相対価格・名目額の両面でヘッジ比率が固定されている。
- **効き幅の見積もり**: -0.05〜+0.05pt。
- **反証条件**: 残差分散が縮小せず、取引コスト増を補えないなら棄却。

### P013 枠: Pair EURUSD/GBPUSD 軸: 入口
- **変更内容**: コード変更。`lookback=200`を100 / 300 / 500にinput化。
- **期待する機構**: 200時間が関係変化に対して短過ぎるか長過ぎるかを確認する、純粋な観測窓軸。
- **効き幅の見積もり**: -0.04〜+0.04pt。
- **反証条件**: 良い一点だけが現れる、または隣接点で符号が安定しないなら採用せず棄却。

### P014 枠: Pair EURUSD/GBPUSD 軸: 入口
- **変更内容**: コード変更。`|z|`が前バーより縮小し始めた時だけ入る `PairRequireZTurning`。
- **期待する機構**: 乖離拡大型の最中ではなく、収束の最初の確認後に入る。既提案だが未測定。
- **効き幅の見積もり**: -0.01〜+0.024pt。
- **反証条件**: 入り遅れによる取り逃しが偽ブレイク回避を上回る、または両窓の片方で悪化。

### P015 枠: Pair EURUSD/GBPUSD 軸: 執行
- **変更内容**: コード変更。2脚の片方が発注失敗した場合、即時に約定脚を解消する原子性ガード。
- **期待する機構**: 片脚だけ残る偶発的な方向リスクを、平均回帰の成績から分離する。収益増より品質・再現性の改善。
- **効き幅の見積もり**: 0〜+0.01pt（正常なテスターではほぼゼロ）。
- **反証条件**: 発注失敗が無く、結果が不変なら「必要な安全策」で止め、収益案としては棄却。

### P016 枠: Carry AUDJPY 軸: 入口
- **変更内容**: コード変更。既存の `hystMult=0.75` をinput化し、0.5 / 1.0だけを比較。
- **期待する機構**: MA上抜けの小さな往復を減らす／または遅れ過ぎを緩和する。退出SMAとは別の入口ヒステリシス軸。
- **効き幅の見積もり**: -0.03〜+0.03pt。ただし7取引/窓なので推定力は極めて低い。
- **反証条件**: 窓末建玉一件を除くと結論が反転する、または少数取引だけで最適点が出るなら棄却。

### P017 枠: Carry AUDJPY 軸: 入口
- **変更内容**: コード変更。`trendPeriod=200`を100 / 300へ変更可能にする。
- **期待する機構**: D1のトレンド定義を変え、長期為替トレンドへの追随と遅行性の釣合いを見る。
- **効き幅の見積もり**: -0.04〜+0.04pt。ただし採用期待は低い。
- **反証条件**: 少数トレードゆえに隣接期間・窓端で符号が変わるなら、値を選ばず軸ごと閉じる。

### P018 枠: Carry AUDJPY 軸: 入口
- **変更内容**: コード変更。既存の退出後クールダウン `cdBars=10` を 0 / 20 にinput化。
- **期待する機構**: MA周辺の再入場往復を抑えるか、逆に良い再追随を取り逃しているかを検証できる。
- **効き幅の見積もり**: -0.02〜+0.02pt。
- **反証条件**: 変更後の差が窓末ポジションの評価額だけで説明できるなら棄却。

### P019 枠: Carry AUDJPY 軸: 執行
- **変更内容**: 既存input `Carry_ExecTF` で H1 と D1 を比較可能。
- **期待する機構**: シグナルはD1のまま、D1確定後の発注タイミングだけが変わる。OANDAでのD1始値発注失敗回避とのトレードオフを測れる。
- **効き幅の見積もり**: -0.02〜+0.02pt。
- **反証条件**: D1で注文拒否・欠落が起きる、または窓端建玉一件に成績が支配されるなら採用しない。

### P020 枠: Carry AUDJPY 軸: 退出・評価
- **変更内容**: コード変更。売買ルールを変えず、窓末のCarry建玉を「最終時点時価」「最後のD1終値」「翌N日」で別掲示する評価機構を追加。
- **期待する機構**: 現在のIS利益の68%が窓末一件という問題を、ルールの良否とテスト終端の偶然から切り分ける。
- **効き幅の見積もり**: 収益改善は0pt。これは採否の信頼性を上げる案。
- **反証条件**: 終端処理を変えるだけで結論が変わるなら、Carryのパラメータ最適化を凍結する。

### P021 枠: PB USDJPY / PB GBPJPY 軸: 退出
- **変更内容**: コード変更。現行ATR SLに対するRRを枠別input化し、PB UJは1.5 / 2.5、PB GJは3 / 5の粗い対称比較。
- **期待する機構**: 入口slopeと独立に、トレンド継続の利得分布に固定TPが合っているかを問える。
- **効き幅の見積もり**: -0.04〜+0.04pt。PB UJのOOSは4往復しかなく、採用期待は低い。
- **反証条件**: 隣接点で安定せず、特にUJで少数取引が結論を支配するなら棄却。

### P022 枠: PB USDJPY / PB GBPJPY 軸: 退出
- **変更内容**: コード変更。+1RでSLを建値、以後はATR 1〜1.5倍のChandelier型トレールにする。
- **期待する機構**: 強いトレンドの押し目では固定TPを超える伸びを残し、失速した再押しを損失化しない。
- **効き幅の見積もり**: -0.03〜+0.03pt。
- **反証条件**: 固定RRより平均利益が落ち、DD改善だけになるなら棄却。

### P023 枠: SCA USDJPY / SCA GBPJPY 軸: 退出
- **変更内容**: コード変更。現行TP（2R）を 1R / 1.5R / 3R に枠別input化。強制決済22時は固定のまま。
- **期待する機構**: 時刻変更ではなく、レンジ幅由来の初期リスクに対する利確距離の適合を調べる。
- **効き幅の見積もり**: -0.05〜+0.05pt。
- **反証条件**: TP到達率の低下を22時強制決済の価格差で補えず、両窓で低下なら棄却。

### P024 枠: SCA USDJPY / SCA GBPJPY 軸: 退出
- **変更内容**: コード変更。+1R到達後のみ建値または+0.25RへSL移動する `ScaBETriggerR` / `ScaBELockR`。
- **期待する機構**: 初動ブレイクが一度伸びてレンジに戻る失敗を小損または小益で終える。時刻・締切・方向フィルタとは独立。
- **効き幅の見積もり**: -0.02〜+0.03pt。
- **反証条件**: SCAの利益の多くが途中の1R逆行を経由するため、BEでTP到達を失うなら棄却。

### P025 枠: SCA GBPJPY 軸: 執行・入口
- **変更内容**: コード変更。レンジ上限・下限からの初回終値の飛び越えをATR比で上限化する `ScaMaxEntryOvershootATR`。
- **期待する機構**: 大幅に飛んだ後の追随ではSLがレンジ反対端のまま遠く、リスクリワードが悪化する。
- **効き幅の見積もり**: -0.01〜+0.04pt。
- **反証条件**: 大飛び越えこそ大トレンドの開始であり、除外により両窓の利益が下がるなら棄却。

### P026 枠: SCA USDJPY / SCA GBPJPY 軸: 退出
- **変更内容**: コード変更。TP未達で一定時間横ばいなら、レンジ中央側へSLを一段寄せる「時間経過型SL縮小」。
- **期待する機構**: ORBは初動で伸びなければ優位性が薄れやすい、という仮説を直接検証する。
- **効き幅の見積もり**: -0.03〜+0.03pt。
- **反証条件**: 22時まで待つこと自体が収益源で、早期SL移動が両窓の利益を削るなら棄却。

優先順位は、実装対効果では `P019`、`P001`、`P024`、`P009`、`P014`、`P012` の順です。ただしP019以外は既存inputだけでは足りず、枠別に切り分けて測るための小規模改造が必要です。全枠一括の`UseProfitTrail`は実行は容易ですが、寄与枠を識別できず、枠ごとの質という今回の主題にはやや不向きです。

率直な見解として、この主題だけで月利6%へ届く見込みは極めて低いです。対照0.428%から必要な差は約+5.57pt/月ですが、今回の候補群で現実的に期待できる合計は、楽観しても+0.1pt/月程度です。さらにPB UJはOOS 4往復、Carryは各窓7取引かつ窓末一件に支配され、Pairも30/56脚なので、大きな改善値ほど偶然を疑うべきです。退出・執行を掘る価値はありますが、目標到達の道筋として期待を置くべきではありません。
