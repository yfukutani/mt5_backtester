# OANDA FX 固定ロット枠のrisk%化 — RSI3枠は効いた。SCA2枠は一度も効いていなかった（2026-09-15）

目標が**月利5% → 月利6%**に変わったことを受けた新ラウンド。
`CLAUDE.md` の段階1（100案出し）→ 段階2（簡易検証）→ 段階3（MT5バックテスト）で進めている。
案は `docs/OANDA_FX_100proposals_20260915.md`（Claude 50案＋Codex 50案）。

---

## 結論（この時点で確定している3つ）

**結論1（バグ）: `FxRiskMask` の SCA ビット（bit3 / bit4）は一度も効いていなかった。**
`experts/MIX_EA_SIMVERIFY.mq5` の `FxRiskOn()` は SCA枠に `useRisk=true` を立てるが、
**SCAの発注経路は `LotRisk()` を通らず固定ロットを直接計算していた。**
そのため `ml/fxrisk1/` の88runのうち、SCAを含む案の結果はすべて
「SCAは固定ロット0.01のまま」の測定値である。2026-09-15 に `ScaBaseLot()` で修正した。

**結論2（有効だった部分）: RSI 3枠の risk%化は実際に効いた。**
fxrisk1 の RSI関連の結果は有効で、**OOS 月利 2.71% → 3.02%**（前ラウンド最良比）。
しかも**最大DDは 57.0% → 41.8% へ改善**している。純益・DDともに前進した。

**結論3: それでも月利6%には遠い。** 88run中、両窓で6%に到達した案は**ゼロ**。
OOS最良が 3.02%/月 で、**目標の約半分**。FULL窓でも 4.63%/月。

---

## 1. バグ — SCAのrisk%化は実装されていなかった

### 実測による証拠

fxrisk1 の FULL窓で、以下の案が**純益・最大DD・取引数まで完全一致**する。

| 案 | マスク | 意味 | FULL純益 | 最大DD | 取引数 |
|---|---:|---|---:|---:|---:|
| R003 | 0 | risk%化なし（対照） | 925,988 | 10.68% | 2,969 |
| R008 | 8 | SCA USDJPY を risk%化**したはず** | 925,988 | 10.68% | 2,969 |
| R009 | 16 | SCA GBPJPY を risk%化**したはず** | 925,988 | 10.68% | 2,969 |
| R016〜R021 | 24 | SCA 2枠を risk%化**したはず**（risk 0.25/0.5/1.0 × fxref 2種） | 925,988 | 10.68% | 2,969 |

risk% を 0.25% から 1.0% まで4倍に振っても**1円も動かない。**
対して RSI のビットは動く（R005 mask=1 → 941,475 / R006 mask=2 → 949,194 /
R007 mask=4 → 943,508）。**SCAのビットだけが完全に無反応**である。

### 原因

```
// ProcSCA() の発注（修正前）
double lot=S[i].lot*GlobalLotMult*S[i].lotMult;   // ← useRisk を見ていない
if(GszApplies(i)){ double gl=GszLot(i,dist); if(gl>0.0) lot=gl; }
```

`GszApplies()` は magic が **PB GOLD (20260640) / SCA GOLD (20261002) のときだけ true** を返す。
FXのSCA枠（20261000 / 20261001）はどちらの経路にも乗らない。

RSI枠は `trade.Buy(LotRisk(i,sld), ...)` と `LotRisk()` を通るので `useRisk` が効いた。
**同じフラグを立てても、枠によって効く枠と効かない枠があった**、という実装の非対称が原因。

Codex にこのラウンドの案出しを依頼したところ、独立に同じ指摘をした
（「SCAは固定数量を直接計算し、Pairは `LotComplex()` を使っています。
したがって6枠すべてを単純なフラグ変更だけでrisk%化することはできません」）。

### 修正

`ScaBaseLot()` を追加し、`GSZ → useRisk(LotRisk) → 固定ロット` の順で解決するようにした。
`useRisk=false` のときは修正前とまったく同じ式を返すので、**既存の測定値は変わらないはず**である
（これを fxrisk2 の S001 / S002 で確認する）。

MetaEditor（XM Trading MT5）でコンパイルし **0 errors / 0 warnings** を確認した。

> [!warning] **`experts/MIX_EA_OANDA.mq5`（本番EA）はまだ修正していない。**
> 本番EAには `FxRiskMask` 自体が無く、SCAは固定ロットのままである。
> 採用が決まってから移植する。

---

## 2. 有効だった部分 — RSI 3枠の risk%化（fxrisk1・88run）

測定条件は前ラウンドと同じ。入金 **500,000円**・レバレッジ25・`every_tick`・
FULL = 2016.11.09〜2026.06.20（**115か月**）／OOS = 2016.11.09〜2021.06.20（**55か月**）。
端末はXMのフィード（前ハーネスの継承。OANDA実フィードでの測定ではない）。

**月利は幾何平均の複利、最大DDは直近ピーク比。**
equity曲線は**決済損益のみ**で作っているため、**実際の最大DDはここの値より深い。**

### SCAビットが無効だったため、下表の mask=31 は実質 mask=7（RSI 3枠のみ）である

| 案 | mask | risk% | FxRiskRefCap | RefCap | 倍率 | FULL純益 | FULL月利 | FULL DD | OOS純益 | **OOS月利** | OOS DD | 最低資産 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| **R001（本番現行）** | 0 | — | — | 78,000 | 1 | 379,084 | 0.49% | 5.6% | 119,537 | **0.39%** | 5.2% | 500,000 |
| R003 | 0 | — | — | 250,000 | 1 | 925,988 | 0.92% | 10.7% | 252,657 | 0.75% | 10.7% | 500,000 |
| R004（前ラウンド推奨C017相当） | 0 | — | — | 250,000 | 3 | 2,837,670 | 1.66% | 27.0% | 782,462 | **1.73%** | 27.0% | 500,000 |
| R005 | 1 (RSI UJ) | 0.5 | 250,000 | 250,000 | 1 | 941,475 | 0.92% | 10.5% | 260,649 | 0.77% | 10.5% | 500,000 |
| R006 | 2 (RSI EU) | 0.5 | 250,000 | 250,000 | 1 | 949,194 | 0.93% | 12.5% | 263,851 | 0.77% | 12.5% | 500,000 |
| R007 | 4 (RSI GU) | 0.5 | 250,000 | 250,000 | 1 | 943,508 | 0.93% | 9.4% | 269,859 | 0.79% | 9.4% | 500,000 |
| R013 | 7 (RSI×3) | 0.5 | **0（複利）** | 250,000 | 1 | 1,398,868 | 1.17% | 18.7% | 370,794 | 1.01% | 13.8% | 500,000 |
| R015 | 7 | 1.0 | **0（複利）** | 250,000 | 1 | 2,126,819 | 1.45% | 39.2% | 557,648 | 1.37% | 27.4% | 500,000 |
| R036 | 7実効 | 0.5 | 0 | **0（全複利）** | 1 | 9,940,644 | 2.68% | 30.6% | 755,318 | 1.69% | 23.1% | 500,000 |
| **R037（OOS最良）** | 7実効 | 0.5 | 0 | **0（全複利）** | **2** | **90,163,318** | **4.63%** | 54.4% | **2,071,976** | **3.02%** | **41.8%** | **500,000** |
| R035 | 7実効 | 0.25 | 0 | 0 | 2 | 55,900,801 | 4.19% | 47.1% | 1,415,411 | 2.47% | 40.0% | 468,293 |
| R033 | 7実効 | 0.1 | 0 | 0 | 2 | 41,068,923 | 3.92% | 42.0% | 1,090,940 | 2.13% | 39.4% | 400,148 |

### 読み取れること

1. **RSI 3枠の risk%化は単独で +0.26ポイント/月**（OOS）。
   R003（0.75%）→ R013（1.01%）。同じ RefCap=250,000・倍率1 での比較。
2. **効くのは「複利にすること」で、「増量」ではない。**
   R012（FxRiskRefCap=250,000 固定＝非複利のrisk%化）は OOS 0.83% にとどまるのに対し、
   R013（FxRiskRefCap=0＝equity連動）は 1.01%。**この差が複利の寄与である。**
   これは Claude案 A2（複利と増量の分離）に対する回答になっている。
3. **前ラウンド最良を上回った。** fxcomp1 の OOS最良 C011（2.71% / 最大DD 57.0%）に対し、
   R037 は **OOS 3.02% / 最大DD 41.8% / 元本割れなし**。
   **利益が上がってDDが下がった**（前ラウンドは「利益を上げるとDDが壊れる」構図だった）。
4. **risk% は高いほうが良いとは限らない。** 全複利×倍率2では
   0.1% → 2.13% / 0.25% → 2.47% / 0.5% → 3.02% と単調だが、
   RefCap=250,000 の系列（R010/R012/R014）では 0.25% と 1.0% の差は 0.24ポイントしかない。
   **効いているのは risk% の水準ではなく「equity連動かどうか」。**

### DDについて（必ず報告する項目）

- 本番現行 R001: **最大DD 5.2%（OOS）/ 5.6%（FULL）**。極端に保守的で、DD予算を使っていない。
- 推奨候補 R037: **最大DD 41.8%（OOS）/ 54.4%（FULL）**。最低資産は500,000円で**元本割れなし**。
- ただし**これは決済損益ベースの下限値**である。建玉中の含み損を含んでいない。
  実運用の最大DDはこれより深く、証拠金維持率による強制決済もこの測定では再現されていない。

---

## 3. まだ足りないもの — 目標6%に対する現在地

| | OOS月利 | 目標6%まで |
|---|---:|---|
| 本番現行（R001） | 0.39% | **15.4倍** |
| 前ラウンド推奨（C017 / R004） | 1.73% | 3.5倍 |
| 今回の最良（R037・RSI risk%化＋全複利＋倍率2） | **3.02%** | **2.0倍** |

**まだ2倍足りない。**

残っている最大の未検証領域が、今回のバグで測れていなかった**SCA 2枠の risk%化**である。
SCAは FULL窓の純益 379,084円のうち **132,331円（35%）**を稼いでおり
（SCA GBPJPY 115,992円 ＋ SCA USDJPY 16,339円）、9枠で最大の稼ぎ頭である。
RSI 3枠（合計 56,860円・15%）の risk%化で OOS +0.26ポイント得られたのだから、
その2.3倍の規模を持つSCAの risk%化には**構造的にもっと大きい余地がある**。

ただし**過大な期待はしないほうがよい。** 理由は2つ。

1. **SCA GBPJPY は `scaBoostMult=6.0`（ドリフト逆行時にロット6倍）を持つ。**
   risk%化するとブースト取引の実効リスクは risk%×6 になる。fxrisk1 が使った
   0.25〜1.0% では 1.5〜6% になり、**破綻側に振れる可能性が高い。**
   fxrisk2 では 0.05% から並べる。
2. **RSIで得た +0.26ポイントは、純益比15%の枠に対する値である。**
   単純に規模で比例させても SCA から +0.6ポイント程度で、3.02% → 3.6% にしかならない。
   **これだけで6%には届かない見込みが高い。**

---

## 4. 進行中 — fxrisk2（30案・60run）

`ml/fxrisk2/`。**SCAが実際に risk% サイジングされる状態での初回測定。**

| family | 内容 | 案数 |
|---|---|---:|
| A | 対照（mask=0 / mask=7）。fxrisk1 の R003 / R013 と**完全一致するはず** | 2 |
| B | SCAを1枠ずつ（mask=8 / 16）× risk 0.05/0.1/0.25 | 6 |
| C | SCA 2枠（mask=24）× risk 0.05〜0.5 × fxref 2種 | 6 |
| D | RSI3＋SCA2（mask=31）非複利 × risk 0.05〜0.5 | 4 |
| E | mask=31 を equity連動（fxref=0） | 3 |
| F | 9枠中8枠を equity連動（RefCap=0）＝最大複利 | 4 |
| G | 最大複利 × 倍率2・3（fxrisk1でOOS最良だった領域） | 5 |

A群は**修正の無影響確認**を兼ねる。S001 が R003（FULL純益 925,988 / DD 10.68% / 2,969取引）と
一致しなければ、修正がSCA以外に波及したことになるので、そこで止める。

所要は 1案あたり約17分（FULL 741秒 ＋ OOS 290秒の実績中央値）で、**30案で約8.6時間**。

### 対照の検証結果 — 修正はSCA以外に影響していない（合格）

S001（FULL）が完了した。純益は **925,988 → 911,736（-14,252・-1.5%）** で、
**数値としては一致しなかった。** ただし原因は修正ではない。

deal ログ 5,938行を1行ずつ突き合わせた結果、**差があるのは2行だけ**だった。

| magic | 枠 | fxrisk1 R003 | fxrisk2 S001 | 差 | 行数 |
|---|---|---:|---:|---:|---:|
| 20260622 | PB USDJPY | 102,468 | 102,468 | **±0** | 154 |
| 20260627 | PB GBPJPY | 183,565 | 183,565 | **±0** | 56 |
| 20260610 | RSI USDJPY | 15,310 | 15,310 | **±0** | 390 |
| 20260605 | RSI EURUSD | 11,835 | 11,835 | **±0** | 1,088 |
| 20260774 | RSI GBPUSD | 29,715 | 29,715 | **±0** | 254 |
| 20260629 | PairTrade | 15,093 | 15,093 | **±0** | 170 |
| 20261000 | SCA USDJPY | 16,339 | 16,339 | **±0** | 1,188 |
| 20261001 | SCA GBPJPY | 115,992 | 115,992 | **±0** | 2,610 |
| **20260650** | **Carry AUDJPY** | **223,074** | **211,209** | **-11,865** | 25 |
| 0 | テスト終了時のCarry建玉の強制決済 | 212,597 | 210,210 | -2,387 | 3 |

**差はすべて Carry AUDJPY に由来する。** 取引数は9枠すべてで完全一致（合計2,969）。

原因は既知の測定系の性質である。MT5テスターは**現在のスワップ率を全履歴へ一律に適用する**ため、
測定した瞬間のAUDJPYロングスワップ値がCarry枠の全期間損益を支配する
（Vault の `MIX_EA (OANDA版)` に記録済み。過去には swapLong 14.11→42.59 の変動だけで
純益が5倍近くブレた実測がある）。fxrisk1 は 2026-09-09、fxrisk2 は 2026-09-15 の測定で、
**この6日間のスワップレート差が -14,252円** として出た。

> [!important] **判定: 合格。** `ScaBaseLot()` は SCA以外の8枠に1円も影響していない。
> ただし**fxrisk1 と fxrisk2 の絶対額をCarry枠込みで直接比較してはいけない。**
> 比較はラウンド内（fxrisk2 の S001 を基準）で行う。
> 月利への影響は FULL 0.918% → 0.909% 程度で、順位づけには影響しない。

---

## 4b. Codex案#43への回答 — 「最大相対DD%」は残高ベースで、含み損を含んでいなかった

これまで各ラウンドで「最大DD」として報告してきた数字には、**2系統あると思っていた**。

1. deal の確定損益から作った equity 曲線のピーク比DD（含み損を含まない**下限値**）
2. MT5テスターの「最大相対DD%」（**含み損を含む本当のDD**だと思っていた）

**この2つは別物ではなかった。**

fxrisk1 の88run すべてで、1と2を突き合わせた結果——

| | |
|---|---|
| テスター相対DD ÷ 決済ベースDD の中央値 | **1.00** |
| 同・最小 | **1.00** |
| 同・最大 | **1.00** |

**88run すべてで小数点以下まで完全一致した。** 偶然ではない。原因はEAの `OnTester()` にある。

```
FileWrite(fh,"max_dd_pct",DoubleToString(TesterStatistics(STAT_BALANCE_DDREL_PERCENT),4));
```

`STAT_BALANCE_DDREL_PERCENT` は**残高（balance）ベース**のドローダウンで、
建玉中の含み損を含まない。MQL5には `STAT_EQUITY_DDREL_PERCENT`（equityベース＝含み損込み）が
別に存在するが、**このプロジェクトでは一度も出力していない。**

> [!warning] **本プロジェクトが報告してきた最大DDは、すべて含み損を含まない下限値である。**
> 「決済損益ベースなので実際のDDはこれより深い」という注記は正しかったが、
> **テスターの値を使えば含み損込みになる、という認識は誤りだった。**
> どちらの経路を使っても同じ残高ベースの値が出ていた。

**修正は1行で済む。** `OnTester()` に `STAT_EQUITY_DDREL_PERCENT` と
`STAT_EQUITY_DD_RELATIVE` を追加すれば、以降の全runで含み損込みDDが追加コストゼロで取れる。

**今は入れない。** `measure.py` は測定中にEAバイナリが差し替わると中断する設計
（`ea_sha()` チェック）で、fxrisk2 の60runが走っている最中だからである。
**fxrisk2 完了後、次ラウンドの最初の作業として入れる。**

これは Codex案 #43「含み損込みの高頻度エクイティで再評価」への回答であり、
Claude案 A9「含み損を考慮したサイジング」の前提条件でもある。

---

## 5. 100案のうち、この時点で判定できるもの

| 案 | 判定 | 根拠 |
|---|---|---|
| **Claude A1**（固定6枠の risk%化） | **一部実証・一部測定中** | RSI3枠は +0.26pt/月（OOS）。SCA2枠は今回のバグで未測定 |
| **Claude A2**（複利と増量の分離） | **実証済み** | R012（非複利）0.83% vs R013（複利）1.01%。効いているのは複利のほう |
| **Claude A6 / D2**（倍率とDD予算の上限） | **一部実証** | 全複利×倍率2で OOS 3.02% / DD 41.8%。倍率3は fxrisk2 G群で測る |
| **Codex #1**（損失予算に基づくrisk%へ移行） | 同上 | Claude A1 と同じ軸 |
| **Codex #2**（SL非依存の資産比例ロット） | **未検証** | Pair枠（`LotComplex`）はこの形。SCA/RSIへの適用は次ラウンド候補 |
| **Codex #43**（含み損込みで再評価） | **原因を特定・修正方法も確定** | EAが `STAT_BALANCE_DDREL_PERCENT` を出力していた。1行で直る（§4b） |
| **Claude C1 / C2**（SCAをEURJPY/AUDJPYへ） | **棄却** | OOS中央値 -13,348 / -5,080円（IS適合）。`oanda_fx_sca_symbol_expansion_20260915.md` |
| **Claude B1**（SCAのレンジ幅フィルタ） | **見立てを下方修正**（§5b） | 純益は **−18%**。1取引の質は上がるが取引を79%捨てる |
| **Claude B2 / B3**（SCAの時間帯・方向フィルタ） | **見立てを上方修正**（§5b） | 純益 +16% / +6.7% で、ポートフォリオ・シャープは +52% / +68% |
| **Codex #49**（25倍・共有証拠金で全体再実行） | **実施済み** | 本ハーネスは統合EAでの測定であり、単体結果の比例拡大ではない |
| **Claude D1**（6%の定義） | **確定** | OOS窓・55か月・幾何平均の複利月利。破綻＝最低資産が入金の20%未満 |

---

## 5b. 自己訂正 — SCAの入口フィルタは B1 ではなく B2 / B3 が本命だった

`docs/OANDA_FX_100proposals_20260915.md` で、Claude案 **B1（SCAのレンジ幅フィルタ）**に
**◎「他プロジェクトで実証済みの唯一の入口フィルタ」**と付けた。**これは読み違いだった。**

X2_HIGH_RISK の V105 が実証したのは**1取引あたりのシャープ**の改善であって、
**純益の改善ではない。** 同じ検証の続き（V106）が、フィルタを実際に適用した純益を出している
（SCA GBPJPY 1枠・弱局面408取引）。

| フィルタ | 残る取引 | 残る割合 | **純益** | 基準比 | 1取引Sの比 | **ポートフォリオS比**（= S比 × √割合） |
|---|---:|---:|---:|---:|---:|---:|
| （なし・基準） | 408 | 100% | **58,541** | — | 1.000 | 1.000 |
| **B1 レンジ幅 上位25%のみ** | 84 | 21% | **47,872** | **−18.2%** | 2.528 | **1.16** |
| **B2 時間帯 IS優位のみ** | 226 | 55% | **68,040** | **+16.2%** | 2.055 | **1.52** |
| **B3 方向 買いのみ** | 221 | 54% | **62,464** | **+6.7%** | 2.288 | **1.68** |
| B1×B2 | 47 | 12% | 43,298 | −26.0% | — | — |
| B1×B2×B3 | 28 | 7% | 52,264 | −10.7% | — | — |

**B1 は純益を18%減らす。** 1取引の質は2.5倍になるが、**取引を79%捨てる**ので総額が負ける。
`S_q/S > 1/√q` の壁（必要120.4%に対し実際152.8%）は**かろうじて通っただけ**で、
ポートフォリオ・シャープの改善は **+16%** にとどまる。

**B2 と B3 は、純益を増やしながらシャープを +52% / +68% 改善する。** こちらが本命である。

### なぜ間違えたか

**X2_HIGH_RISK と OANDA FX では目的関数が違う。**
X2 の目的は「期限内の到達率」と「不毛期間をなくすこと」で、そこでは1取引の質が効く。
**OANDA FX の目的は月利＝純益**である。V105 の表（1取引S）だけを見て◎を付け、
**同じラウンドのV106（純益）を見ていなかった。**

### それでもフィルタに意味がある理由 — risk%化との組み合わせ

固定ロット0.01では、取引を捨てて空いたリスク予算を**再投入する手段がない**。
だから B1 は単純に減益になる。

**risk%化すると話が変わる。** シャープが上がった分だけ risk% を引き上げれば、
**同じDD予算でより多くのリターン**を取れる。上の表の「ポートフォリオS比」がそのまま
**同DDでの利益倍率の上限**になる（B3なら最大1.68倍）。

**これが次ラウンド（fxrisk3）の設計になる: SCAの入口フィルタ × risk%サイジングの同時適用。**
単独ではなく組み合わせでしか効かない、という構造を前提に測る。

> [!warning] 数値の出どころに注意
> 上表は X2_HIGH_RISK の窓定義（IS窓／弱局面）での SCA GBPJPY 単枠の値であり、
> 本ラウンドの FULL/OOS とは窓が違う。**同じ枠（magic 20261001）ではあるが、
> OANDA FX の9枠ブックでそのまま再現する保証はない。段階3で測り直すこと。**

---

## 6. 生成物

- `experts/MIX_EA_SIMVERIFY.mq5` — `ScaBaseLot()` を追加（SCAのrisk%化バグ修正）
- `ml/fxrisk1/adjudicate.py` — 複利前提の評価（fxcomp1から流用・目標6%・基準R001）
- `ml/fxrisk2/generate_proposals.py` / `proposals.csv` — 30案
- `ml/fxrisk2/measure.py` — 60run（案ごとにFULL→OOSを続けて測る順に変更）
- `ml/fxrisk2/adjudicate.py` — 同上
- `docs/OANDA_FX_100proposals_20260915.md` — Claude 50案＋Codex 50案
